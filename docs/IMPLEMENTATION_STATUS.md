# Implementation Status

Recorded: 2026-07-16
Reference plan: [MIGRATION_PLAN.md](MIGRATION_PLAN.md)

Source translation: [ERP_TRANSLATION_MAP.md](ERP_TRANSLATION_MAP.md)

This is an implementation ledger, not a replacement-readiness claim. The
existing ERP remains authoritative.

## Completed slices

| Slice | Package | Evidence |
|---|---|---|
| Runtime language boundary | `docs/MIGRATION_PLAN.md` + `scripts/pure_moonbit_runtime_paths.txt` + `scripts/company_no_python_runtime_gate.sh` | The approved target is pure MoonBit plus shell orchestration. The allow-listed runtime manifest is checked by a shell gate that rejects Python interpreter/module invocations in supported `cmd`, Rabbita, PostgreSQL, gateway, service, smoke, and source-inventory paths. Existing Python services, gateways, migration adapters, source probes, and smokes are frozen comparison evidence only; no new Python surface is allowed. Native MoonBit replacements must preserve routing, authorization, response envelopes, receipts, and replay behavior before each legacy bridge is archived. |
| No-Python release gate | `scripts/company_no_python_runtime_gate.sh` + `scripts/pure_moonbit_runtime_paths.txt` | The shell-only gate passes for the current supported manifest and fails closed on missing paths, unsafe manifest entries, Python interpreter/module calls, or `.py` executable references. Legacy Python rehearsal shells are intentionally outside the manifest until their MoonBit replacements are complete. |
| Remaining ERP mutation boundaries | `cmd/postgres_company_service/boundary_candidates.mbt` + `cmd/postgres_company_service/auth_commands.mbt` + `cmd/postgres_company_service/import_commands.mbt` + `cmd/postgres_company_service/cbs_r0_commands.mbt` + `cmd/postgres_company_service/cbs_demo_commands.mbt` + `cmd/postgres_company_service/cbs_change_commands.mbt` + `cmd/postgres_company_service/sales_customer_commands.mbt` + `cmd/postgres_company_service/sales_subscription_commands.mbt` + `cmd/postgres_company_service/sales_mortgage_commands.mbt` + `cmd/postgres_company_service/sales_refund_commands.mbt` + `scripts/company_postgres_boundary_smoke.sh` + `ERP_AUTH_RUNTIME_AUDIT.md` + `ERP_IMPORT_RUNTIME_AUDIT.md` + `ERP_CBS_RUNTIME_AUDIT.md` + `ERP_SALES_RUNTIME_VERTICAL.md` | Native MoonBit recognizes every previously unported source mutation family. Logout/profile are persisted signed commands with PostgreSQL revisions, audit receipts, replay, and `/auth/me` readback. Signed project and contract batch imports now validate source references and commit command-owned PostgreSQL projections with list readback and replay; exact project/contract CSV template downloads are native. Sales customer create/update/delete, subscription create/convert, mortgage create/approve/release, and refund create/approve now use signed command projections and source-shaped merge; customer deletion tombstones only command-owned rows, while subscription conversion, mortgage release, and refund approval explicitly leave downstream contract/revenue reconciliation pending. CBS R0 resolution now records a signed, dictionary-aware, auditable local intent with imported-contract protection and explicit pending state when source dictionaries are absent. CBS demo-contract create/state/legacy/clear/action commands now persist protected, budget-check-pending local projections and merge into readback; native change registration and lifecycle now persist protected, replayable `cbs_change` revisions through `estimated → approving → confirmed`, with workflow, budget, accounting, cash, and tax effects disabled. Login/password and finance-authorized downstream effects remain explicit authenticated 409/approval gates; the parity ledger has zero `not_connected` API handlers. |
| Legal-entity validation | `foundation` | Entity ID, name, and currency validation tests. |
| Local RBAC and delegated authority directory | `foundation/access` | Versioned roles, exact-scope permissions, principal/actor assignments, assignment caps, revocation, effective-dated delegation, bounded `AuthorityGrant` issuance, and exact-scope separation-of-duties rules are implemented and tested; the reviewed native access importer now exercises this boundary while real source role rows remain absent. |
| Organization hierarchy | `foundation/organization` | Business-unit/company hierarchy, parent validation, duplicate protection, and scoped activation. |
| ERP business-unit promotion | `migration/erp` + `foundation/organization` | Source business units require explicit principal/scope mappings and per-unit creation grants; parent-before-child ordering and orphan rejection are enforced. |
| Evidence and audit trail | `foundation/evidence` | Provenance/hash-backed evidence lifecycle and append-only audit replay protection. |
| Scoped authority | `foundation` | Principal/actor/capability/scope/amount checks and denial tests. |
| Fixed-point money | `finance` | Minor-unit arithmetic and currency checks. |
| Chart of accounts and period book | `finance/accounting` | Account ownership, period open/soft-close/close, currency/account checks, ledger posting gate, and a reconciled-close method that refuses to close without balanced subledger/control reports. |
| Accounting event links | `finance/accounting` | Source aggregate → validated journal links with append-only replay protection by both event identity and source aggregate identity. |
| Accounting event envelopes | `finance/accounting` + `persistence/store` | Validated source-to-journal links serialize to schema-bound records for reconciliation and migration batches. |
| Journal invariant | `finance` | Balanced, single-currency, positive-posting validation. |
| Append-only ledger boundary | `finance` | In-memory append-only posting contract with duplicate-entry rejection. |
| Versioned schema contract | `persistence/schema` | Forward-only sequential migration steps with checksum and replay rejection. |
| Record-store and snapshot adapter | `persistence/store` | Append/read envelope plus deterministic JSON snapshot, file round-trip, and validated backup/restore path with source identity and schema migration checks. |
| Atomic migration batches | `persistence/store` | All-or-nothing batch append contract with duplicate and empty-batch guards. |
| Durable file transaction journal | `persistence/store` | Pending-snapshot write-ahead path, validated reopen/recovery, and source-identity enforcement; a production database still owns physical durability guarantees. |
| Aggregate projection revisions | `persistence/store` | Immutable aggregate snapshots with expected-revision sequencing, source-event idempotency, latest-revision lookup, and snapshot reconstruction. |
| SQL migration catalog and command boundary | `persistence/sql` | Versioned up/down DDL for the generic envelope plus aggregate projections, accounting links, and migration receipts; parameterized insert commands, duplicate guards, explicit commit/rollback plans, and executors against both the immutable record store and durable file boundary. A production database driver remains outside this package. |
| SQLite durable migration rehearsal | `cmd/sqlite_rehearsal` + `scripts/company_sqlite_rehearsal.sh` | Native MoonBit validates the redacted staging manifest/file hash, raw identity/payload/secret boundaries, schema checksums, canonical payloads, and cohort receipt hashes; it emits the transaction-bound SQLite SQL plan, reopens for integrity/count verification, and makes an identical replay idempotent. This remains a rehearsal adapter, not the production service driver. |
| SQLite accounting-link adapter | `cmd/sqlite_accounting_link_apply` + `scripts/company_sqlite_accounting_link_apply.sh` | Native MoonBit validates reviewed accounting-link receipts, emits conflict-checked SQLite SQL, preserves event/source/journal/principal identity, finalizes `AccountingLinked` receipts, verifies scoped hashes after reopen, and preserves zero-insert replay semantics. SQLite accounting cohorts now use the shell adapter; journal posting, cash release, tax, and period close remain separate gates. |
| SQLite accounting-link parity | `cmd/sqlite_accounting_link_parity` + `scripts/company_sqlite_accounting_link_parity.sh` | Native MoonBit compares the reviewed receipt with reopened SQLite identity rows and emits the same `shadow_verified`/mismatch envelope; SQLite accounting cohort shells no longer invoke the Python parity checker. |
| SQLite accounting reconciliation | `cmd/sqlite_accounting_reconciliation` + `scripts/company_sqlite_accounting_reconciliation.sh` | Native MoonBit validates domain/plan/receipt envelopes, quarantined-item state, principal/amount/currency continuity, durable link identity, integrity, and the historical reconciliation report. SQLite migration/accounting shells now invoke this adapter. |
| Shared SQLite driver boundary | `scripts/company_sqlite_driver.py` (comparison evidence) + `cmd/sqlite_driver_smoke` + `cmd/sqlite_backup_restore` + native shell wrappers | The native smoke and backup paths centralize the reviewed WAL, foreign-key, busy-timeout, immediate-transaction, rollback, reopen, integrity, online-backup, logical-digest, and parameterized company-record contracts. The Python driver remains frozen comparison evidence. This is the local service-driver prototype, not a managed production database deployment. |
| PostgreSQL target adapter | `cmd/postgres_target_apply` + `scripts/company_postgres_target_apply.sh` + `persistence/postgres` + `scripts/postgres_target_schema.sql` | Native MoonBit now validates the redacted raw-staging manifest/NDJSON, writes a temporary CSV for PostgreSQL `\\copy`, applies the version-4 catalog with conflict checks and transaction-bound inserts, computes logical row hashes, finalizes the durable migration receipt, and verifies idempotent replay. The cohort rehearsal invokes the shell wrapper rather than Python; a clean isolated PostgreSQL schema inserted 120 rows and a native replay inserted 0 with the same applied hash as the Python bridge. The local target contains additional accumulated rehearsal rows; reviewed projection and accounting cohorts remain separate adapters. |
| PostgreSQL aggregate projection adapter | `cmd/postgres_projection_apply` + `scripts/company_postgres_projection_apply.sh` + `persistence/postgres` + `scripts/company_postgres_projection_apply.py` | Native MoonBit now validates domain-promotion receipts, derives deterministic source-event IDs/canonical payloads, persists immutable aggregate revisions with table locking and conflict checks, finalizes cohort-scoped `Projected` receipts, and verifies replay. The primary typed cohort rehearsal and dedicated cohort wrappers invoke the shell wrapper; a clean isolated PostgreSQL schema inserted 19 projections and a native replay inserted 0 with the same applied hash as the Python bridge. Cash, accounting posting, and target ownership remain separate gates. |
| PostgreSQL accounting-link adapter | `cmd/postgres_accounting_link_apply` + `scripts/company_postgres_accounting_link_apply.sh` + `persistence/postgres` + `scripts/company_postgres_accounting_link_apply.py` | Native MoonBit now validates accounting-link receipts, enforces event/source/journal uniqueness, persists explicit traceability rows with table locking/conflict checks, finalizes cohort-scoped `AccountingLinked` receipts, and verifies replay. All PostgreSQL cohort rehearsal wrappers invoke the shell entrypoint; a clean isolated schema inserted two links and a native replay inserted 0 with matching hash to the Python bridge. It never posts journals, releases cash, or closes periods. |
| PostgreSQL exact projection parity | `cmd/postgres_exact_projection_parity` + `scripts/company_postgres_exact_projection_parity.sh` | One native MoonBit verifier now replaces the nine duplicate Python exact-candidate parity checks for opening controls, tax, bank, financing, assets, treasury, invoice, procurement, and investment-performance cohorts. It validates cohort/type allow-lists, compares multiplicity and canonical candidate payloads, and preserves each historical parity envelope. |
| SQLite exact projection parity | `cmd/sqlite_projection_parity` exact mode + `scripts/company_sqlite_exact_projection_parity.sh` | The SQLite projection parity command now has an exact-candidate mode matching the native PostgreSQL verifier. Migration rehearsal checks for the nine reviewed cohorts compare multiplicity and canonical candidate payloads without Python. |
| Cross-domain projection parity | `scripts/company_cross_domain_projection_parity.sh` + native exact SQLite/PostgreSQL verifiers | The cross-domain rehearsal now enumerates domain-promotion receipts, runs the native exact candidate verifier against both targets, and aggregates the historical cross-domain report without Python. Verifying both targets against the same canonical receipt is stronger than a backend-only identity check. |
| Mapped ERP promotion planner | `cmd/promotion_plan` + `scripts/erp_promotion_plan.sh` | Native MoonBit now compiles the five mapped source tables (`mu_business_unit`, `ep_project`, `cb_contract`, `cb_cost`, `vcb_loan_simple`) with explicit principal/counterparty/employee/currency mappings, half-even money policy, parent/project checks, deterministic quarantine reasons, warnings, and the exact `moonproj.erp.promotion-plan.v1` envelope. The migration and PostgreSQL cohort rehearsals no longer invoke the Python planner. |
| Reviewed accounting-posting boundary | `scripts/erp_accounting_post_plan.sh` + `cmd/accounting_post_plan` + `cmd/accounting_post` + `ERP_ACCOUNTING_POSTING.md` | The reviewed chart/period compiler is now native MoonBit and preserves linked-event identity, explicit chart/account-kind/currency checks, approved-event allow-listing, and the `moonproj.erp.accounting-posting-plan.v1` envelope. Native `AccountingBook.post` validates balanced journals, account/currency/period/authority invariants, and emits an `accounting-posting-v1` projection. SQLite/PostgreSQL parity and zero-insert replay pass for two source-bound commitment journals; cash release, tax filing, period close, opening balances, and production ownership remain separate gates. |
| Reviewed opening-control boundary | `scripts/erp_opening_control_plan.sh` + `cmd/opening_control_plan` + `cmd/opening_control` + `migration/control` + `ERP_OPENING_CONTROLS.md` | The reviewed-map compiler is now native MoonBit and preserves the Python contract: recursive secret-key rejection, metric uniqueness, non-negative values/tolerances, and the `moonproj.erp.opening-control-plan.v1` envelope. An explicit reviewed map preserves five synthetic control totals with value, tolerance, unit, and dimension; native shadow expectations compile, and SQLite/PostgreSQL exact candidate parity plus zero-insert replay pass. The boundary is reconciliation evidence only: no inferred opening balance, ledger posting, cash release, tax filing, period close, or owner acceptance. |
| Reviewed tax-filing boundary | `scripts/erp_tax_filing_plan.sh` + `cmd/tax_filing_plan` + `cmd/tax_filing` + `finance/tax` + `ERP_TAX_FILING.md` | The reviewed-map compiler is now native MoonBit and preserves the Python contract: recursive secret-key rejection, source/filing uniqueness, amount/rate bounds, final-state allow-list, and the `moonproj.erp.tax-filing-plan.v1` envelope. Two synthetic reviewed obligations exercise native calculation, review, filing submission, and accepted/rejected outcomes; exact SQLite/PostgreSQL candidate parity and zero-insert replay pass. Tax payment, accounting posting, cash release, period close, external authority calls, and production source acceptance remain separate. |
| Reviewed bank-statement boundary | `scripts/erp_bank_statement_plan.sh` + `cmd/bank_statement_plan` + `cmd/bank_statement` + `finance/treasury` + `ERP_BANK_STATEMENT.md` | The reviewed-map compiler is now native MoonBit and preserves the Python contract: recursive secret-key rejection, source/statement/line uniqueness, positive line amounts, inflow/outflow allow-list, and the `moonproj.erp.bank-statement-plan.v1` envelope. One synthetic two-line statement passes native balance/currency/scope/identity validation, exact SQLite/PostgreSQL candidate parity, and zero-insert replay. Movement matching, ledger reconciliation, cash release, provider access, accounting posting, and production source acceptance remain separate. |
| Reviewed financing-facility boundary | `scripts/erp_financing_facility_plan.sh` + `cmd/financing_facility_plan` + `cmd/financing_facility` + `finance/financing` + `ERP_FINANCING_FACILITY.md` | The reviewed-map compiler is now native MoonBit and preserves the Python contract: recursive secret-key rejection, facility uniqueness, positive limit/range checks, draw/repayment bounds, expected-state allow-list, and the `moonproj.erp.financing-facility-plan.v1` envelope. One synthetic facility passes native approval/activation/draw/repayment/interest lifecycle validation, exact SQLite/PostgreSQL candidate parity, and zero-insert replay. Lender calls, cash disbursement/settlement, accounting posting, tax treatment, covenant tracking, period close, and production source acceptance remain separate. |
| Reviewed asset-lifecycle boundary | `scripts/erp_asset_lifecycle_plan.sh` + `cmd/asset_lifecycle_plan` + `cmd/asset_lifecycle` + `finance/assets` + `ERP_ASSET_LIFECYCLE.md` | The reviewed-map compiler is now native MoonBit and preserves the Python contract: recursive secret-key rejection, asset/state uniqueness and bounds, impairment/disposal consistency, depreciation-period uniqueness, account-map validation, and the `moonproj.erp.asset-lifecycle-plan.v1` envelope. One synthetic asset passes native capitalization/activation/depreciation/disposal lifecycle and balanced journal validation, exact SQLite/PostgreSQL candidate parity, and zero-insert replay. Journal posting, disposal cash settlement, tax treatment, period close, and production source acceptance remain separate. |
| Reviewed treasury plan/dispatch boundary | `scripts/erp_treasury_plan_dispatch_plan.sh` + `cmd/treasury_plan_dispatch_plan` + `cmd/treasury_plan_dispatch` + `finance/treasury` + `ERP_TREASURY_PLAN_DISPATCH.md` | The reviewed-map compiler is now native MoonBit and preserves the Python contract: recursive secret-key rejection, plan/dispatch uniqueness, direction/state allow-lists, planned/actual amount bounds, project separation, and the `moonproj.erp.treasury-plan-dispatch-plan.v1` envelope. Two synthetic cash plans and one fund dispatch pass native confirmation/actualization/approval/execution validation, exact SQLite/PostgreSQL candidate parity, and zero-insert replay. Bank movement, cash release, settlement, accounting/tax treatment, period close, and production source acceptance remain separate. |
| Reviewed invoice/subledger boundary | `scripts/erp_invoice_subledger_plan.sh` + `cmd/invoice_subledger_plan` + `cmd/invoice_subledger` + `operations/invoice` + `finance/receivable` + `finance/payable` + `ERP_INVOICE_SUBLEDGER.md` | The reviewed-map compiler is now native MoonBit and preserves the Python contract: recursive secret-key rejection, invoice/receivable/payable uniqueness, payment/state consistency, positive amounts, and the `moonproj.erp.invoice-subledger-plan.v1` envelope. Two synthetic customer invoices open two receivables and one supplier payable; native issue/accept/collection/payment validation, exact SQLite/PostgreSQL candidate parity, and zero-insert replay pass for five projections. Cash release, revenue/expense posting, tax settlement, period close, and production source acceptance remain separate. |
| Reviewed procurement cohort | `scripts/erp_procurement_cohort_plan.sh` + `cmd/procurement_cohort_plan` + `cmd/procurement_cohort` + `operations/procurement` + `operations/commitment` + `ERP_PROCUREMENT_COHORT.md` | The reviewed-map compiler is now native MoonBit and preserves the Python contract: recursive secret-key rejection, supplier/tender/commitment uniqueness, supplier evaluation/state allow-lists, bid count/currency/amount checks, award-to-bid continuity, and the `moonproj.erp.procurement-cohort-plan.v1` envelope. Two synthetic suppliers, one two-bid tender, and one performed commitment pass native qualification/award/obligation validation, exact SQLite/PostgreSQL candidate parity, and zero-insert replay for four projections. Cash release, accounting posting, settlement, tax, period close, and production source acceptance remain separate. |
| Investment performance boundary | `scripts/erp_investment_performance_plan.sh` + `cmd/investment_performance_plan` + `cmd/investment_performance` + `investment/portfolio` + `ERP_INVESTMENT_PERFORMANCE.md` | The reviewed portfolio/quote/benchmark compiler is now native MoonBit and preserves the Python contract: recursive secret-key rejection, exposure/position/quote uniqueness and bounds, currency continuity, benchmark identity, and the `moonproj.erp.investment-performance-plan.v1` envelope. Two synthetic positions pass mandate/quote/performance attribution and an external benchmark observation reconciles within tolerance; exact SQLite/PostgreSQL candidate parity and zero-insert replay pass for three analytics-only projections. Position mutation, cash release, accounting posting, period close, and production feed acceptance remain separate. |
| Investment valuation accounting boundary | `scripts/erp_investment_valuation_plan.sh` + `cmd/investment_valuation_plan` + `cmd/investment_valuation` + `scripts/company_accounting_link_parity.py` + `ERP_INVESTMENT_VALUATION.md` | The reviewed portfolio/quote compiler is now native MoonBit and preserves performance-plan identity, secret-key rejection, explicit portfolio continuity, gain/loss direction, journal identity, and the `moonproj.erp.investment-valuation-plan.v1` envelope. The native domain command creates one explicit CNY 100,000 mark-to-market gain event; a separate accounting map binds event, journal, accounts, scope, principal, amount, and currency. SQLite/PostgreSQL identity parity and zero-insert replay pass for one durable link. Position mutation, accounting-book posting, cash release, period close, and production feed acceptance remain separate. |
| Invoice/procurement accounting-link boundary | `scripts/company_invoice_procurement_accounting_rehearsal.sh` + `scripts/erp_accounting_link_plan.py` + `scripts/company_accounting_link_parity.py` + `ERP_INVOICE_PROCUREMENT_ACCOUNTING.md` | Target-specific reviewed keys bind two receivable openings, one payable opening, and one performed procurement commitment; native validation, exact SQLite/PostgreSQL identity parity, and zero-insert replay pass for four links. Cash settlement, accounting-book posting, tax, period close, and production source acceptance remain separate. |
| Reviewed sales/receivables cohort | `scripts/erp_sales_cohort_plan.sh` + `cmd/sales_cohort_plan` + `cmd/sales_cohort` + `scripts/company_sales_cohort_rehearsal.sh` + `ERP_SALES_COHORT.md` | The reviewed sales compiler is now native MoonBit and preserves customer/subscription/agreement amount and currency continuity, mortgage/refund bounds, source-evidence-only revenue, recursive secret-key rejection, and the exact `moonproj.erp.sales-cohort-plan.v1` envelope. One reviewed synthetic customer, converted subscription, fulfilled sales agreement, opened receivable, released mortgage lifecycle, paid refund workflow, and source-evidence-only revenue row persist as seven projections with exact SQLite/PostgreSQL parity and zero-insert replay. Collection, refund cash, revenue recognition, accounting posting, period close, and production source acceptance remain separate. |
| Reviewed marketing cohort | `scripts/erp_marketing_cohort_plan.sh` + `cmd/marketing_cohort_plan` + `cmd/marketing_cohort` + `scripts/company_marketing_cohort_rehearsal.sh` + `ERP_MARKETING_COHORT.md` | The reviewed marketing compiler is now native MoonBit and preserves campaign/placement budget continuity, completed/placed state requirements, catalog-only channel/material evidence, recursive secret-key rejection, and the exact `moonproj.erp.marketing-cohort-plan.v1` envelope. One bounded campaign, one placed allocation, and channel/material catalog evidence persist as four projections with exact SQLite/PostgreSQL parity and zero-insert replay. Provider calls, budget-ledger consumption, cash release, accounting posting, attribution, period close, and production source acceptance remain separate. |
| PostgreSQL marketing command runtime | `scripts/company_postgres_service.py` + `scripts/company_postgres_dev_gateway.py` + `scripts/company_postgres_service_smoke.py` + `scripts/company_postgres_dev_gateway_smoke.py` + `scripts/erp_ui_parity_matrix.py` | Source-shaped marketing reads now merge imported rows with PostgreSQL-owned campaign, placement, channel, and material projections. Authority-bound create/update/delete/effect commands persist idempotent receipts, aggregate revisions, and audit events; deterministic defaults make replay stable, and gateway smoke covers trusted-session forwarding. Imported rows remain read-only; provider execution, CBS/budget consumption, accounting/tax, attribution, production identity, and owner acceptance remain gated. |
| Reviewed contract milestone/settlement cohort | `scripts/erp_contract_milestone_plan.sh` + `cmd/contract_milestone_plan` + `cmd/contract_milestone` + `scripts/company_contract_milestone_rehearsal.sh` + `ERP_CONTRACT_MILESTONE_COHORT.md` | The reviewed cohort compiler is now native MoonBit and preserves the Python contract: source-table/identity checks, state/currency/amount continuity, trigger/rate bounds, uniqueness, and the `moonproj.erp.contract-milestone-cohort-plan.v1` envelope. One performed commitment drives one progress milestone through eligible/reached and creates one milestone-linked requested settlement. Three projections pass exact SQLite/PostgreSQL parity and zero-insert replay; approval, release, cash, accounting, tax, period close, and production source acceptance remain separate. |
| Reviewed employee expense/advance-offset cohort | `scripts/erp_expense_advance_cohort_plan.sh` + `cmd/expense_advance_cohort_plan` + `cmd/expense_advance_cohort` + `scripts/company_expense_advance_cohort_rehearsal.sh` + `ERP_EXPENSE_ADVANCE_COHORT.md` | The reviewed cohort compiler is now native MoonBit and preserves the Python contract: recursive secret-key rejection, source-table/identity checks, employee/principal/currency continuity, allocation totals, advance/offset bounds, and the `moonproj.erp.expense-advance-cohort-plan.v1` envelope. One employee advance, one approved allocated expense, and one bounded offset persist as three projections with exact SQLite/PostgreSQL parity and zero-insert replay. The offset changes only the advance balance; recognition, cash, accounting, tax, period close, and production expense acceptance remain separate. |
| Employee advance/offset accounting-link cohort | `scripts/company_expense_advance_accounting_rehearsal.sh` + `scripts/company_postgres_accounting_reconciliation.py` + `scripts/fixtures/expense_advance_accounting_link_mapping.example.json` + `cmd/accounting_link` | Separate reviewed issuance and offset journal links pass native validation, exact SQLite/PostgreSQL identity parity, zero-insert replay, and source-to-durable reconciliation for two links. The expense claim remains outside this link slice to avoid duplicate recognition; book posting, cash, tax, period close, and owner acceptance remain separate. |
| Tax/financing accounting-link boundary | `scripts/company_tax_financing_accounting_rehearsal.sh` + `scripts/erp_tax_accounting_plan.sh` + `scripts/erp_financing_accounting_plan.sh` + `cmd/tax_accounting_plan` + `cmd/financing_accounting_plan` + `cmd/tax_accounting_link` + `cmd/financing_accounting_link` + `ERP_TAX_FINANCING_ACCOUNTING.md` | The reviewed tax and financing accounting compilers are now native MoonBit and preserve tax/facility snapshot continuity, recursive secret-key rejection, fixed-point amounts, explicit source/principal/scope/amount/currency identity, journal direction, and the exact tax-accounting and financing-accounting plan envelopes. Separate reviewed maps bind two tax-obligation recognition events plus one financing draw and one repayment event; exact SQLite/PostgreSQL identity parity and zero-insert replay pass for four links. Tax filing/payment, lender calls, cash release, accounting-book posting, period close, and production source acceptance remain separate. |
| PostgreSQL typed-cohort rehearsal | `scripts/company_postgres_cohort_rehearsal.sh` + native `cmd/postgres_projection_parity`/`cmd/postgres_exact_projection_parity` wrappers | Re-runs raw staging, the 19-item base domain receipt, ten core typed cohorts, the source-bound investment-model evaluation, and optional CBS, workflow-assignment, delivery-progress, reviewed delivery-recognition, advance-offset, payment-accounting, source budget, source warning, notification, access, reviewed accounting-posting, opening-control, tax-filing, bank-statement, financing-facility, asset-lifecycle, treasury-plan/dispatch, invoice/subledger, procurement, investment-performance, contract-milestone, and expense/advance cohorts against PostgreSQL, compares every receipt by target/source identity, and replays each cohort. The demonstrated full rehearsal reaches 142 aggregate projections and 7 accounting links; exact parity and zero-insert replay pass for every supplied cohort. Reviewed delivery recognition remains opt-in because the available report has no accepted value. |
| Cross-domain projection parity | `scripts/company_cross_domain_projection_parity.sh` + native exact SQLite/PostgreSQL parity wrappers + `scripts/company_postgres_cohort_rehearsal.sh` | After both targets are reopened, the shell/native path compares every domain-promotion receipt’s expected identities and canonical candidate payloads across the isolated SQLite rehearsal and PostgreSQL target. The demonstrated task-state/full cohort reports `shadow_verified` for all 13 receipts, including the quarantined task-state observation, with no cross-domain missing/extra identities or payload mismatches. The former Python checker is frozen comparison evidence only. |
| Rabbita ERP visual port (partial) | `frontend/main` + `frontend/public` + `frontend/README.md` + `ERP_EXPENSE_BROWSER_ACCEPTANCE.md` + `ERP_CONTRACT_RUNTIME_VERTICAL.md` + `ERP_PAYMENT_APPLICATION_RUNTIME_VERTICAL.md` + `ERP_PROCUREMENT_RUNTIME_VERTICAL.md` + `ERP_SALES_RUNTIME_VERTICAL.md` + `ERP_DELIVERY_RUNTIME_AUDIT.md` | Designer-facing ERP shell is ported with Rabbita 0.12.4 and Warren-compatible JS build output: source login gradient/copy, 220/64px dark navigation, nested ERP menu hierarchy, header actions, dashboard KPI/funnel/risk layout, and mobile drawer behavior. Major ERP route families render source-shaped fixtures for projects/plans/workflow, AI, sales, cost/procurement, finance, analysis, and system administration; representative project, contract, expense, loan, and supplier detail/new flows open as responsive forms. Inbox, attachments, health, users/roles, profile, notifications, OCR, and webhook routes have distinct source-shaped screens, and the report center opens a read-only `/share/:token`-shaped preview. The native gateway/service now carries the expense, contract, payment-application, sales-revenue, tender/contract-split, marketing, supplier/provider, invoice/tax, fund-plan/dispatch, delivery progress/output/task-report, and project-plan task read/command loops; the project-plan shell and task report path are covered by the native service and trusted-gateway smokes. The former Python gateway is frozen comparison evidence only. Imported procurement, sales, delivery, marketing, invoice, and fund/task rows remain read-only; browser production-identity acceptance and managed deployment remain pending. |
| Native MoonBit gateway/session boundary | `cmd/postgres_company_gateway` + `scripts/company_postgres_gateway.sh` + `scripts/company_postgres_gateway_smoke.sh` + `ERP_PROFILE_RUNTIME_AUDIT.md` | Native MoonBit now keeps the service bearer token private, establishes an HttpOnly session, signs the actor assertion, forwards only allow-listed company paths, serves the Warren bundle with SPA fallback, and supports the short-lived trusted-upstream HMAC identity mode. The shell-only smoke covers development login/session/logout, native summary and signed expense-command forwarding, command allow-list rejection, secure trusted login, and PostgreSQL profile verification. The in-memory session store, issuer/audience validation, persistent rotation, managed deployment, and owner approval remain open; the Python gateway is frozen comparison evidence only. |
| ERP dashboard/cockpit runtime gate | `cmd/postgres_company_service/dashboard.mbt` + `scripts/company_postgres_dashboard_smoke.sh` + `frontend/main` + `docs/ERP_DASHBOARD_RUNTIME_AUDIT.md` | Native MoonBit now serves the seven dashboard GET surfaces over 30 source tables: v1 overview/funnel/anomalies, project KPI/anomalies, scoped v2, and scoped v3. PostgreSQL coverage and missing-table metadata are returned on every response, with authorizing/persisted/provider flags false; the shell smoke proves the imported values (2 projects, 2 contracts, ¥5.64m paid, seven funnel stages, v3 expense-by-city). The native implementation is intentionally bounded: sales, fund, invoice, tender, warning, CBS, and workflow source tables remain empty in the controlled export, so full v3 parity, production identity, browser scope, and owner reconciliation are still gates. |
| ERP dashboard v2/v3 and admin diagnostics | `cmd/postgres_company_service/dashboard.mbt` + `scripts/company_postgres_dashboard_smoke.sh` + `scripts/erp_ui_parity_matrix.py` + `ERP_DASHBOARD_RUNTIME_AUDIT.md` | Native MoonBit owns the scoped v2 KPI/payment-trend envelope with imported plan/application months, stage distribution, warning rows, unpaid amount, and running-process counts, plus a source-shaped v3 aggregate with scoped sales/contract/fund/invoice/tender/progress metrics, expense categories/cities, health/gauge/funnel/compare panels, and top contract panels. All sections expose coverage and explicit non-authorizing metadata. Admin health now summarizes imported BPM instances/statuses/recent activity; LLM/AI diagnostics remain source-safe configuration boundaries, runtime metrics are unavailable, secrets are redacted, and provider execution is disabled. Production identity, full cross-domain v3 coverage, browser acceptance, and owner reconciliation remain pending. |
| ERP core reporting runtime | `cmd/postgres_company_service/reports.mbt` + `cmd/postgres_company_service/report_share_commands.mbt` + `cmd/postgres_company_service/report_export.mbt` + `scripts/company_postgres_source_read_smoke.sh` + `scripts/company_postgres_report_share_smoke.sh` + `scripts/company_postgres_report_export_smoke.sh` + `scripts/company_postgres_report_export_gateway_smoke.sh` + `frontend/main` + `ERP_REPORT_RUNTIME_AUDIT.md` | The five source core reports now have native PostgreSQL reads and one Rabbita `/reports` overview: cost summary, contract-payment ledger, supplier analysis, approval efficiency, and project-stage matrix. Source-table coverage is returned explicitly; missing supplier/workflow rows therefore produce empty source-backed sections rather than fabricated data. Native share create/list/revoke and public metadata/data reads now use a bounded command projection with forwarded-TLS and access-count evidence. Native `POST /api/company/export/excel` emits a bounded source-compatible multi-sheet XLSX from the authenticated service with pure MoonBit ZIP/XML; Rabbita `/ai-stats` now downloads a bounded AI-summary workbook through the native gateway. Production share credentials/managed serving, PDF export, browser production identity, and owner reconciliation remain separate gates. |
| ERP workflow-definition runtime | `cmd/postgres_company_service/workflow_defs.mbt` + `scripts/company_postgres_source_read_smoke.sh` + `frontend/main` + `ERP_WORKFLOW_RUNTIME_AUDIT.md` | Native MoonBit now exposes two active process definitions, twelve ordered steps, and six assignee mappings with imported user labels through `/api/company/workflow/process-defs` and preview. Rabbita `/tasks` shows the definitions while explicitly reporting zero imported workflow instances/actions; local command-owned workflow actions are separate from source-definition synchronization. |
| Workflow definition read boundary | `cmd/postgres_company_service/workflow_commands.mbt` + `scripts/company_postgres_workflow_smoke.sh` + `scripts/company_postgres_workflow_gateway_smoke.sh` + `frontend/main` + `scripts/company_postgres_service.py` (frozen comparison evidence) + `scripts/company_postgres_dev_gateway.py` | The authenticated local service preserves source process-definition/preview reads and binds signed local workflow start/approve/reject/cosign/transfer commands to PostgreSQL projections; Rabbita `/tasks` now exposes those five bounded candidates through the trusted gateway, with replay/readback smoke coverage. Imported workflow rows remain read-only and source engine synchronization, delegation semantics, and production identity remain gated. |
| ERP workflow instance reads and local task actions | `cmd/postgres_company_service` + `cmd/postgres_company_service/workflow_commands.mbt` + `scripts/company_postgres_workflow_smoke.sh` + `scripts/company_postgres_source_read_smoke.sh` + `frontend/main` + `ERP_WORKFLOW_RUNTIME_AUDIT.md` | Native MoonBit serves source-compatible `/tasks/mine`, `/tasks/initiated`, `/tasks/my-history`, `/instances/by-biz`, and `/instances/:piGuid` reads, with canonical and source aliases. Signed `POST` start/approve/reject/cosign/transfer aliases persist command-owned workflow-instance/action revisions, audit receipts, replay equality, local owner checks, and explicit workflow-only/no-provider/cash/accounting/tax effects; active command rows merge into readback while imported `wf_process_instance`/`wf_step_action` rows remain read-only. The workflow smoke proves start/replay/pending/readback/approve/reject/cosign/transfer. Source process-engine synchronization, full assignee/delegation semantics, browser acceptance, production identity, and workflow-owner approval remain separate gates. |
| Native admin/notification/AI/RBAC observation wave | `cmd/postgres_company_service/observations.mbt` + `cmd/postgres_company_service/rbac_commands.mbt` + `scripts/company_postgres_source_read_smoke.sh` + `scripts/company_postgres_rbac_smoke.sh` + `frontend/main` | Native MoonBit now owns attachment metadata/download boundaries, AI statistics and AI-hub empty/source reads, webhook configuration, notification messages/subscriptions/configuration/digest/provider reads, admin OCR/error/dictionary/health/quality/LLM diagnostics and gated backup boundary, RBAC users/me/roles/permission catalog, and report-template list/meta/run boundary reads. Signed local RBAC role upsert/delete, user-role assignment, user create/update/toggle, and password-reset candidates now persist redacted reviewed security candidates with replay receipts; imported identity remains read-only and command projections never authorize requests. All responses preserve PostgreSQL source coverage, redaction, `authorizing=false`, and provider/query execution disabled markers; credential binding, password/identity writes, and production authorization binding remain gated. |
| ERP warning observation runtime | `cmd/postgres_company_service/warning.mbt` + `cmd/postgres_company_service/warning_ticket_commands.mbt` + `scripts/company_postgres_warning_smoke.sh` + `scripts/company_postgres_warning_gateway_smoke.sh` + `scripts/company_postgres_source_read_smoke.sh` + `frontend/main` + `ERP_WARNING_RUNTIME_AUDIT.md` | Native MoonBit now evaluates imported project/cost warning observations, including the controlled export's W005 finding, badge, rule summary, source/command custom-rule readback, signed dry-run custom-rule/scan previews, warning-to-ticket candidate with `/tickets/mine` readback, and ticket status/reassign/extend lifecycle candidates. Rabbita `/warning` now exposes ticket create/status/reassign/extend controls through the trusted gateway. Signed resolve/ignore commands persist command-owned warning-state overlays; signed super-user rule enable/disable and custom-rule create/delete candidates persist redacted `warning_rule_config`/`warning_custom_rule` overlays with replay receipts and audit events. Ticket candidates persist assignee/due-date/note metadata and lifecycle revisions with actor checks and audit receipts; completing a local ticket appends the warning resolution overlay. No ticket command notifies, webhooks, or invokes a provider. Custom SQL is validated/hashed but never executed; previews never persist findings or dispatch notifications. Production scans, external delivery, identity, and owner acceptance remain gated. |
| ERP project master/plan runtime | `cmd/postgres_company_service` + `cmd/postgres_company_service/project_commands.mbt` + `cmd/postgres_company_gateway` + `cmd/postgres_company_service/plan_ai_commands.mbt` + `scripts/company_postgres_project_plan_smoke.sh` + `scripts/company_postgres_source_read_smoke.sh` + `scripts/company_postgres_gateway_smoke.sh` + `frontend/main` + `ERP_PROJECT_RUNTIME_AUDIT.md` | Native MoonBit PostgreSQL reads expose imported and command-owned projects, fourteen lifecycle instances, nine tasks, and task-report evidence through `/api/company/projects` and detail. Source-compatible lifecycle/plan reads expose ordered stages, task list, task detail/report history, key-node summary, and delay-impact evidence for the same imported rows. Local MDM project create/update/delete aliases and project-plan task create/update/delete plus evidence-gated report commands persist separate PostgreSQL projections with authority, idempotency, revisions, and audit receipts; imported project/task rows remain read-only and no workflow/cash/accounting/tax effect is inferred. The pure-shell project-plan smoke now covers MDM alias replay/update/tombstone and the seven-node deterministic AI suggestion candidate; Rabbita `/project-plan` exposes and renders that candidate through the trusted gateway, while `/projects` and `/projects/:guid` show live rows with the designer layout retained as an explicit fallback. Provider execution, browser acceptance, production identity, and operations-owner approval remain separate gates. Python service/gateway files remain frozen comparison evidence only. |
| ERP MDM organization/project runtime | `cmd/postgres_company_service/project_commands.mbt` + `cmd/postgres_company_service/budget_catalog.mbt` + `cmd/postgres_company_service` + `scripts/company_postgres_project_plan_smoke.sh` + `scripts/company_postgres_source_read_smoke.sh` + `scripts/erp_ui_parity_matrix.py` + `ERP_MDM_RUNTIME_AUDIT.md` | Source-compatible `/api/company/business-units/tree` preserves one group, two companies, four departments, parent/child hierarchy, legal names, and source identities from seven imported `mu_business_unit` rows. `POST /api/company/source/mdm/projects` plus `PUT`/`DELETE .../:guid` translate the ERP project field family into command-owned source-shaped projections with idempotent replay, lifecycle initialization, audit receipts, imported-row protection, and explicit no-task/workflow/budget/accounting/cash/tax effects. Legal-principal ownership, production identity, browser acceptance, and named MDM/operations sign-off remain separate gates. |
| ERP budget dictionary/runtime reads and expense commands | `cmd/postgres_company_service` + `cmd/postgres_company_service/budget_commands.mbt` + `cmd/postgres_company_service/budget_auto_offset.mbt` + `scripts/company_postgres_budget_expense_smoke.sh` + `scripts/company_postgres_expense_smoke.sh` + `frontend/main` + `ERP_BUDGET_RUNTIME_AUDIT.md` + `ERP_EXPENSE_RUNTIME_VERTICAL.md` | Native MoonBit serves source-compatible cost-subject/proceedings reads, imported `/api/company/budget/expenses` list/detail over `vcb_expense`, `cb_expense_detail`, and `cb_expense_split`, and `POST /api/company/budget-check` as a calculation-only, non-authorizing `cb_cost` headroom preview. Signed source-shaped budget expense create/update/submit-for-approval/void commands now persist local expense/detail/split projections, enforce draft/applicant transitions, protect imported rows, and merge source-shaped readback with replay/audit evidence. `POST /api/company/budget/expenses/:guid/auto-offset` now computes a FIFO approved-loan plan and updates only command-owned Draft expense amounts; it does not mutate loan balances or consume budget. The budget-expense smoke covers create/replay/detail/auto-offset/replay/readback/update/submit/void. Dictionary mutation, budget reservation/consumption, workflow-engine synchronization, production identity, and finance-owner acceptance remain separate gates. |
| ERP budget scope/loan-balance reads | `cmd/postgres_company_service` + `scripts/company_postgres_service.sh` + `scripts/company_postgres_source_read_smoke.sh` + `frontend/main` + `ERP_BUDGET_RUNTIME_AUDIT.md` | Native MoonBit now serves source-compatible `/api/company/source/budget/users-in-bu` and `/my-loan-balance`, preserving four enabled users in `bu-tjgs-0001`, the 3,500.00 approved loan balance for `limingjin`, explicit source coverage/scope flags, and non-authorizing metadata. Live responses are parsed-equal to the frozen bridge; the shell smoke verifies the native path. Rabbita expense list/new surfaces show the imported scope and balance provenance; no scope grant, budget reservation, or loan mutation is inferred. |
| ERP cost contract/payment source reads | `cmd/postgres_company_service` + `scripts/company_postgres_service.sh` + `scripts/company_postgres_source_read_smoke.sh` + `frontend/main` + `ERP_CONTRACT_RUNTIME_VERTICAL.md` + `ERP_PAYMENT_APPLICATION_RUNTIME_VERTICAL.md` | Native MoonBit now serves the command-owned and source-compatible contract/payment observations: `/api/company/contracts`, `/api/company/payment-applies`, `/api/company/source/cost/contracts[/{id}[/milestones]]`, and `/api/company/source/cost/payment-applies`, including filters, detail plans/applies/milestones, source coverage, URL-decoded query aliases, and explicit no-effect markers. Live native responses are parsed-equal to the frozen Python bridge for lists, details, milestone-empty boundaries, keyword/BU/project filters, and payment views. The shell-only smoke covers the native boundary without Python. Imported rows remain read-only; command projections carry provenance; cash release, accounting, tax, production identity, and owner acceptance remain gated. |
| ERP contract source mutation aliases | `cmd/postgres_company_service` + `scripts/company_postgres_service.sh` + `scripts/company_postgres_contract_smoke.sh` + `scripts/company_postgres_gateway_smoke.sh` + `ERP_CONTRACT_RUNTIME_VERTICAL.md` | Native MoonBit `POST /api/company/source/cost/contracts` plus `PUT`/`DELETE .../:guid` reuse the native contract projection for idempotent source-field create/update/void commands, while direct `/api/company/contracts` lifecycle routes preserve submit/reject/resubmit/approve transitions. The shell-only service smoke covers replay, source update, all lifecycle transitions, source-shaped detail, and tombstone readback; the gateway smoke covers signed source create forwarding. BU/project/provider/amount/CBS fields, source-shaped response/readback, and command provenance are preserved. Updates are limited to command-owned draft/submitted contracts; imported contracts remain read-only and void is a local tombstone. CBS/budget validation, accounting, cash, tax, browser acceptance, production identity, and owner approval remain separate gates. |
| ERP contract-milestone source mutation aliases | `cmd/postgres_company_service` + `scripts/company_postgres_service.sh` + `scripts/company_postgres_milestone_smoke.sh` + `ERP_CONTRACT_RUNTIME_VERTICAL.md` | Native MoonBit `POST /api/company/source/cost/milestones` and `POST /api/company/source/cost/contracts/:guid/milestones` translate one or a bounded batch of source milestones into deterministic `contract_milestone_command` projections; `PUT`/`DELETE /api/company/source/cost/milestones/:guid` update or tombstone command-owned rows; and `POST .../trigger-event` reaches pending event nodes. Source-shaped contract detail/readback merges command rows with explicit provenance; imported contracts/milestones remain read-only and all command effects are cash/accounting/tax-neutral. The shell-only smoke proves health capabilities, replay, source detail, update, trigger, and delete. Payment execution, CBS/budget/workflow enforcement, browser acceptance, production identity, and owner approval remain separate gates; `scripts/company_postgres_service.py` is frozen comparison evidence only. |
| ERP payment-application source mutation aliases | `cmd/postgres_company_service` + `scripts/company_postgres_service.sh` + `scripts/company_postgres_payment_smoke.sh` + `scripts/company_postgres_gateway_smoke.sh` + `ERP_PAYMENT_APPLICATION_RUNTIME_VERTICAL.md` | Native MoonBit `POST /api/company/source/cost/payment-applies` translates ERP fields into an idempotent local create plus submit workflow; direct payment routes preserve submit/reject/resubmit/approve transitions, and `PUT`/`DELETE` aliases update or void only command-owned rows. The shell-only payment smoke covers replay, update, imported-row protection, approval lifecycle, and void; the gateway smoke covers signed source create forwarding. Source-shaped readback carries `source_kind=command`, command projection, and explicit no-cash/accounting/tax markers. Payment release, imported-row edits, browser acceptance, production identity, and finance-owner approval remain separate gates. |
| ERP invoice/tax runtime | `cmd/postgres_company_service` + `scripts/company_postgres_service.sh` + `scripts/company_postgres_invoice_smoke.sh` + `scripts/company_postgres_gateway_smoke.sh` + `scripts/company_postgres_source_read_smoke.sh` + `scripts/company_postgres_service.py` (frozen bridge) + `frontend/main` + `ERP_INVOICE_RUNTIME_AUDIT.md` | Native MoonBit serves `/api/company/receivables[/{id}]`, `/api/company/invoices[/{id}]`, `/api/company/source/invoice/{in,out}`, and `/tax-ledger`, and now owns source invoice POST create plus DELETE tombstone commands in both directions. The command boundary validates signed finance authority, fixed-point totals/tax, replay equality, imported-row protection, immutable revisions, receipts, and audit events; source-shaped reads merge command projections with `sourceKind=command`. The invoice shell smoke covers authority, fallback IDs, replay/collision, tax-ledger readback, and tombstones; gateway smoke covers trusted forwarding. OCR/verification, tax filing, accounting, cash, production identity, and owner acceptance remain gated; Python is frozen comparison evidence only. |
| ERP delivery/progress runtime reads and commands | `cmd/postgres_company_service` + `cmd/postgres_company_gateway` + `scripts/company_postgres_delivery_smoke.sh` + `scripts/company_postgres_source_read_smoke.sh` + `scripts/company_postgres_gateway_smoke.sh` + `frontend/main` + `ERP_DELIVERY_RUNTIME_AUDIT.md` | Native MoonBit serves source-compatible `/api/company/source/delivery/{progress,outputs}` plus merged `/api/company/delivery/{progress,outputs,tasks,task-reports,plan-summary,overview}` reads. Evidence-gated progress create/report/accept/reject/delete, output create/confirm, and task-report commands persist idempotent PostgreSQL receipts, immutable revisions, audit events, and explicit no-delivery/cash/accounting/tax markers; imported rows remain read-only. Native service, source-read, delivery, and trusted-gateway smokes cover replay, transitions, readback, and tombstoning. Rabbita `/project/progress` and `/project-plan` consume the native delivery overview and native project-plan task boundary; browser production identity and named owner acceptance remain open. |
| ERP sales source reads and revenue commands | `cmd/postgres_company_service` + `scripts/company_postgres_service.sh` + `scripts/company_postgres_sales_revenue_smoke.sh` + `scripts/company_postgres_gateway_smoke.sh` + `scripts/company_postgres_source_read_smoke.sh` + `scripts/company_postgres_service.py` (frozen bridge) + `frontend/main` + `ERP_SALES_RUNTIME_VERTICAL.md` | Native MoonBit serves source-compatible `/api/company/source/sales/{customers,subscriptions,contracts,mortgages,refunds,revenues}`, normalizes aggregate identity for Rabbita, preserves filters/amount display/defaults, merges command-owned revenue/refund projections, and reports all six source-table counts with non-authorizing/non-persisting markers. Native revenue create/update/confirm/delete and refund create/approve commands require signed actors/authority where applicable, enforce replay, protect imported rows, persist immutable revisions/audit receipts, and read back with `source_kind=command`; Rabbita `/sales/customers`, `/sales/subscriptions`, `/sales/mortgages`, and `/sales/revenues` now expose bounded local lifecycle candidates with explicit no-downstream-effect status. The sales-revenue shell smoke proves revenue authority/replay/update/confirmation/readback/tombstone; the gateway smoke covers customer tombstone, subscription creation, mortgage create/approve/release, and refund create/approve forwarding. Conversion, refund settlement, revenue recognition, collection/cash, accounting/tax, browser acceptance, production identity, and sales/finance-owner approval remain gated; Python is frozen comparison evidence only. |
| ERP tender source reads and create/state/award/split/delete aliases | `cmd/postgres_company_service` + `cmd/postgres_company_service/tender_reads.mbt` + `scripts/company_postgres_service.sh` + `scripts/company_postgres_tender_smoke.sh` + `scripts/company_postgres_source_read_smoke.sh` + `scripts/company_postgres_gateway_smoke.sh` + `scripts/company_postgres_service.py` (frozen bridge) + `frontend/main` + `ERP_PROCUREMENT_RUNTIME_VERTICAL.md` | Native MoonBit serves source-compatible `/api/company/source/tender/{tenders,awards,splits}` and direct `/api/company/{tenders,tender-splits}` list/detail reads, preserving the three source families, command-owned tender/award/split readback, filters, identity/display fields, fixed-point amount/percent formatting, coverage, and cash/accounting/tax-neutral markers. Native tender planning/lifecycle, source tender create/state/award/split aliases, replay, imported-row protection, and tombstones use immutable command projections and audit receipts. Rabbita `/tender` now exposes source state-advance and award candidates through the trusted gateway; the tender shell smoke covers source state/award aliases and award readback in addition to lifecycle, source readback, split creation, and tombstones, and the gateway smoke covers qualified-supplier setup plus trusted state/award forwarding. Arbitrary state overwrite, hard-delete, imported-row deletion, production identity, browser acceptance, award-to-commitment, and procurement-owner approval remain separate gates; Python is frozen comparison evidence only. |
| ERP tender source mutation aliases | `cmd/postgres_company_service` + `scripts/company_postgres_tender_smoke.sh` + `scripts/company_postgres_gateway_smoke.sh` + `scripts/company_postgres_service.py` (frozen bridge) + `frontend/main` + `ERP_PROCUREMENT_RUNTIME_VERTICAL.md` | Native `POST /api/company/source/tender/tenders` and `/splits` translate `projGuid`/`tenderName`/fixed-point amount/date fields and `parentContractGuid`/split amount/percent fields; `PUT .../tenders/:guid/state` maps only through the forward-only native graph; and `POST .../awards` requires provider identity, matching bid evidence, and an active qualified supplier. These aliases persist idempotent PostgreSQL tender/award/contract-split projections and source-shaped readback. Native `DELETE /api/company/source/tender/tenders/:guid` and `/api/company/tenders/:guid` tombstone only command-owned rows; arbitrary state overwrite, hard-delete, award-to-commitment, imported-row mutation, and production owner acceptance remain gates. |
| ERP marketing source reads and commands | `cmd/postgres_company_service` + `scripts/company_postgres_marketing_smoke.sh` + `scripts/company_postgres_source_read_smoke.sh` + `scripts/company_postgres_gateway_smoke.sh` + `frontend/main` + `ERP_MARKETING_COHORT.md` | Native MoonBit serves `/api/company/marketing/{campaigns,placements,channels,materials}` with source coverage, project/state/campaign filters, command provenance, campaign/channel joins, placement totals, and explicit no-provider/cash/accounting/tax markers. Campaign, placement, channel, and material create commands plus campaign update, placement effect, and campaign/channel/material tombstones require signed authority, normalize fixed-point amounts, protect imported rows, persist immutable revisions/audit receipts, and enforce request-equal idempotent replay. The marketing shell smoke covers all four resources, readback, update/effect, replay, and tombstones; gateway smoke covers trusted campaign forwarding. Provider execution, budget-ledger consumption, attribution, accounting/tax, production identity, browser acceptance, and finance-owner approval remain separate gates; Python is frozen comparison evidence only. |
| ERP supplier provider CRUD, dictionary/signature boundary, and rescore command | `cmd/postgres_company_service` + `cmd/postgres_company_service/supplier_reads.mbt` + `cmd/postgres_company_service/supplier_rescore_commands.mbt` + `cmd/postgres_company_service/supplier_analytics.mbt` + `scripts/company_postgres_service.sh` + `scripts/company_postgres_supplier_smoke.sh` + `scripts/company_postgres_gateway_smoke.sh` + `scripts/company_postgres_source_read_smoke.sh` + `scripts/company_postgres_service.py` (frozen bridge) + `scripts/erp_ui_parity_matrix.py` + `frontend/main` + `ERP_PROCUREMENT_RUNTIME_VERTICAL.md` | Native MoonBit owns direct supplier create/update/submit-review/review/blacklist/void and source provider POST/PATCH/PUT/DELETE aliases with signed actors, idempotency, immutable revisions, audit receipts, source-shaped readback, and imported-row protection. Canonical `/api/company/suppliers` list/detail/risk reads now preserve the bridge's fixed projection fields and persisted command risk fields; source SRM envelopes remain available separately. Signed `/providers/rescore-all` computes the ERP formula, persists score/rating/tags only on command-owned supplier projections, protects imported rows, and records replayable command/audit receipts; gateway and supplier smokes cover replay and risk-board readback. Provider list/detail/risk observations, statistics overview, source risk-board distribution/high-risk reads, command risk-board readback, and dictionaries preserve coverage/provenance and missing-provider semantics. Populated-source signature checks/qualification, source-row promotion, provider execution, browser identity, and owner sign-off remain gated; Python is frozen comparison evidence only. |
| ERP investment runtime reads, lifecycle, and explanation candidate | `cmd/postgres_company_service/investment.mbt` + `cmd/postgres_company_service/investment_actual.mbt` + `cmd/postgres_company_service/investment_commands.mbt` + `cmd/postgres_company_service/investment_excel.mbt` + `cmd/postgres_company_service/ai_explain_commands.mbt` + `scripts/company_postgres_source_read_smoke.sh` + `scripts/company_postgres_investment_smoke.sh` + `scripts/company_postgres_investment_gateway_smoke.sh` + `scripts/company_postgres_investment_actual_smoke.sh` + `scripts/company_postgres_ai_explain_smoke.sh` + `scripts/company_postgres_gateway_smoke.sh` + `frontend/main` + `ERP_INVESTMENT_RUNTIME_AUDIT.md` | Native MoonBit now serves the source-compatible investment version, grouped-index, profit-summary, dimensions, sensitivity, bounded actual-vs-plan `profit-actual`, Excel-import list/detail/bridge/preview/profit-table/plan-line reads, the explicit binary-upload gate, signed dry-run Excel index-upsert, plan-line-import, subject-mapping, and plan-line-edit candidates, plan-lines, subject-mappings, profit-cockpit, cost-dashboard observation boundaries, and a signed deterministic `/ai-explain` candidate. Rabbita `/investment` now exposes the explanation, Excel upload, subject-mapping, plan-line edit, Excel index-upsert, Excel plan-line import, and version/index lifecycle candidates and renders their bounded results through the trusted gateway. Actual profit uses imported receipts, paid applications, expenses, contracts, and loans, and labels sparse plan fallback values as simulated; all analytics remain read-only with no budget, workflow, provider, cash, accounting, or tax effect. Signed local version/index lifecycle commands persist idempotent command/audit projections, protect imported rows, and merge command readback. Excel parser, non-dry-run index/plan-line/mapping/edit writes, valuation, provider execution, browser identity, formula reconciliation, and investment-owner acceptance remain separate gates. |
| ERP dynamic-cost runtime read | `cmd/postgres_company_service` + `scripts/company_postgres_service.sh` + `scripts/company_postgres_source_read_smoke.sh` + `frontend/main` + `ERP_DYNAMIC_COST_RUNTIME_AUDIT.md` | Native MoonBit now serves source-compatible `/api/company/cost/dynamic-cost` and `/api/company/source/cost/dynamic-cost/:id/remarks`, preserving seven `cb_cost` rows, the source A/B/C/D/E/F/G/H formula, command projection merge, and one cost-subject remark observation. Live responses match the frozen bridge, including rounded summary metrics and missing-remark 404 semantics; the shell-only smoke covers the native boundary. Rabbita `/dynamic-cost` renders the imported hierarchy, formula columns, remarks, and summary with fixture fallback. Remarks writes, CBS ownership, budget consumption, accounting, cash, tax, browser identity, and owner acceptance remain separate gates. |
| ERP dynamic-cost source mutation aliases | `cmd/postgres_company_service` + `scripts/company_postgres_service.sh` + `scripts/company_postgres_dynamic_cost_smoke.sh` + `scripts/company_postgres_gateway_smoke.sh` + `ERP_DYNAMIC_COST_RUNTIME_AUDIT.md` | Native MoonBit `POST /api/company/cost/dynamic-cost` translates ERP source fields into an idempotent `dynamic_cost_command` projection; `PUT` and `DELETE /api/company/source/cost/dynamic-cost/:guid` update or tombstone only command-owned rows. Native validation preserves project scope, fixed-point amounts, imported-row read-only protection, request-equal replay, immutable revisions, audit receipts, source-shaped readback, and explicit no-effect markers. Shell-only service and gateway smokes cover create/replay/update/imported guard/void. Budget/CBS enforcement, accounting, cash, tax, browser acceptance, production identity, and operations-owner approval remain separate gates. |
| ERP cost-dashboard v3 runtime read | `cmd/postgres_company_service/investment.mbt` + `scripts/company_postgres_source_read_smoke.sh` + `frontend/main` + `ERP_COST_DASHBOARD_V3_RUNTIME_AUDIT.md` | Native MoonBit now owns source-compatible `/api/company/investment/projects/:id/profit-actual-v2`, preserving the R/l2/l3 hierarchy and B=D+E+F+G/H calculations with explicit CBS/version coverage. The controlled export has no `cb_subject_dict` or `cb_plan_version` rows, so Rabbita `/cost-dashboard-v3` renders a successful empty-source state; budget, accounting, cash, tax, and owner acceptance remain gated. |
| ERP CBS runtime read and version/approval commands | `cmd/postgres_company_service/cbs.mbt` + `cmd/postgres_company_service/cbs_commands.mbt` + `cmd/postgres_company_service/cbs_batch_adjust.mbt` + `cmd/postgres_company_service/cbs_approval_commands.mbt` + `cmd/postgres_company_service/cbs_r0_commands.mbt` + `cmd/postgres_company_service/cbs_demo_commands.mbt` + `cmd/postgres_company_service/cbs_change_commands.mbt` + `scripts/company_postgres_cbs_smoke.sh` + `scripts/company_postgres_cbs_approval_smoke.sh` + `scripts/company_postgres_source_read_smoke.sh` + `scripts/company_postgres_boundary_smoke.sh` + `frontend/main` + `ERP_CBS_RUNTIME_AUDIT.md` | Native MoonBit owns source-compatible non-authorizing R master, project dictionary, F-balance, version/compare, R0 queue, approval-rule, change, and source-contract reads. Signed local version clone/freeze/activate, dictionary create, draft dictionary batch-adjust, R0 resolution, demo-contract create/state/legacy/clear/action, and change registration plus `estimated → approving → confirmed` lifecycle aliases now persist idempotent command/audit projections, merge command readback, protect imported rows, and retain explicit pending/no-effect markers. The workflow engine is not invoked. |
| ERP fund-plan runtime read/commands | `cmd/postgres_company_service` + `scripts/company_postgres_fund_smoke.sh` + `scripts/company_postgres_source_read_smoke.sh` + `scripts/company_postgres_gateway_smoke.sh` + `frontend/main` + `ERP_FUND_RUNTIME_AUDIT.md` | Native MoonBit serves source-compatible project/period fund plans, gap analysis, and dispatch evidence with command provenance and explicit no-cash/accounting/tax markers. Native plan create/update/delete and dispatch create/approve commands require signed authority where applicable, deterministic idempotency, immutable revisions, audit receipts, imported-row protection, and creator/state guards. The fund shell smoke covers replay, update, gap analysis, dispatch approval, readback, and tombstoning; gateway smoke covers trusted plan forwarding. The controlled export has no imported fund rows, so Rabbita `/fund/plan` preserves the explicit empty-source state with designer fallback. Bank settlement, cash release, accounting, tax, production identity, browser acceptance, and finance-owner approval remain separate gates; Python is frozen comparison evidence only. |
| ERP warning runtime reads and local state commands | `cmd/postgres_company_service/warning.mbt` + `cmd/postgres_company_service/warning_ticket_commands.mbt` + `cmd/postgres_company_gateway` + `scripts/company_postgres_warning_smoke.sh` + `scripts/company_postgres_warning_gateway_smoke.sh` + `scripts/company_postgres_source_read_smoke.sh` + `frontend/main` + `ERP_WARNING_RUNTIME_AUDIT.md` | Native MoonBit now serves source-compatible warning observations and signed `POST /api/company/warning/:guid/{resolve,ignore,to-ticket}` commands plus `/tickets/mine` readback and signed `PATCH /tickets/:id/{status,reassign,extend}` transitions. The trusted gateway forwards resolve/ignore and the Rabbita warning view can issue the bounded local action; `/warning` now exposes the scan-preview candidate and `/warning-rules` exposes the custom-rule SQL preview candidate. Both previews are bounded, never execute or persist queries, and do not send notifications; gateway smoke proves authenticated forwarding. Resolve/ignore persist a command-owned `warning_state` overlay; to-ticket and lifecycle transitions persist `warning_ticket` revisions with assignee/due-date/note/handled metadata, receipts, and audit events. Completing a local ticket appends the warning resolution overlay. Readback exposes idempotent replay plus `authorizing=false`, with no provider/notification/webhook/cash/accounting/tax effects. Rule writes, scans, external delivery, production identity, and owner acceptance remain separate gates. |
| ERP supplier/provider and risk runtime | `cmd/postgres_company_service/supplier_rescore_commands.mbt` + `cmd/postgres_company_service/supplier_check_sign.mbt` + `cmd/postgres_company_gateway` + `scripts/company_postgres_supplier_smoke.sh` + `scripts/company_postgres_tender_smoke.sh` + `scripts/company_postgres_gateway_smoke.sh` + `frontend/main` | Native MoonBit serves source-backed supplier/provider and risk observations plus signed supplier lifecycle commands and the source-compatible `/providers/:guid/check-sign` boundary. Imported providers return the procurement-owner gate; command-owned suppliers receive a derived, non-authorizing risk preview with no persistence or provider execution. Rabbita `/srm/providers` exposes global risk-rescore and provider detail exposes signature-check preview through the trusted gateway; replay/readback smoke proves imported rows remain protected, local projections are persisted only where intended, and provider execution/cash/accounting/tax effects remain disabled. |
| ERP RBAC user/role runtime | `cmd/postgres_company_service/rbac_commands.mbt` + `cmd/postgres_company_gateway` + `scripts/company_postgres_rbac_smoke.sh` + `frontend/main` | Native MoonBit preserves source-backed users, roles, assignments, and permission-catalog reads plus signed RBAC candidates. Rabbita `/users` now exposes local-user and local-role creation, local-role assignment, user profile update, user toggle, password-reset, and local-role deletion candidates through the trusted gateway; service and temporary gateway smokes prove credential redaction, idempotent replay, imported-identity/system-role protection, `authorizing=false`, and no provider/cash/accounting/tax effect. |
| ERP attachment runtime metadata and mutation candidates | `cmd/postgres_company_service/observations.mbt` + `cmd/postgres_company_service/attachment_commands.mbt` + `cmd/postgres_company_gateway` + `scripts/company_postgres_source_read_smoke.sh` + `scripts/company_postgres_attachment_smoke.sh` + `scripts/company_postgres_attachment_gateway_smoke.sh` + `frontend/main` + `ERP_ATTACHMENT_RUNTIME_AUDIT.md` | Native MoonBit now owns source-backed `/list`, `/all`, and `/stats` filters/counts/uploader/AI metadata, source aliases, the missing-record `/download/:guid` boundary, and signed upload/delete/re-extract candidates with attachment provenance, explicit `downloadable=false`, and `binary_storage=not_imported`. The trusted gateway forwards upload, dynamic OCR re-extraction, and dynamic delete candidates; Rabbita `/attachments` exposes upload, download-boundary, OCR re-extraction, and delete controls and displays the source-404/no-provider/no-delete boundary when the controlled export is empty. The gateway smoke proves authenticated 409 upload/delete and 404 re-extract/download forwarding with no persistence, binary access, or provider execution. Multipart bytes, object-store ownership, real binary download/deletion, provider-backed OCR, retention, production identity, and owner acceptance remain separate gates. |
| ERP loan workflow synchronization candidate | `cmd/postgres_company_service/loan.mbt` + `cmd/postgres_company_gateway` + `scripts/company_postgres_loan_smoke.sh` + `frontend/main` + `ERP_UI_PARITY_MATRIX.md` | Rabbita `/loans` and local loan detail now expose the native `sync-from-workflow` boundary. The candidate requires the existing loan command identity/idempotency path, returns the existing 409 when `wf_process_instance` source rows are unavailable, and leaves loan state, approval, cash, accounting, tax, and workflow effects unchanged. Source workflow rows, owner acceptance, and production identity remain separate gates. |
| ERP notification runtime reads and user command overlays | `cmd/postgres_company_service/notification_commands.mbt` + `cmd/postgres_company_service/notification_dispatch_commands.mbt` + `cmd/postgres_company_service/notification_email_commands.mbt` + `cmd/postgres_company_gateway` + `scripts/company_postgres_notification_smoke.sh` + `scripts/company_postgres_notification_gateway_smoke.sh` + `cmd/postgres_company_service/observations.mbt` + `frontend/main` + `ERP_NOTIFICATION_RUNTIME_AUDIT.md` | Native MoonBit now serves source-compatible user-scoped messages/unread counts and subscriptions, redacted configuration status, source-backed email-outbox metadata, digest preview/log, and LLM-provider discovery over PostgreSQL. Signed-user subscription `POST` plus `PATCH`/`DELETE`, message `read`/`read-all` aliases, a super-user, allow-listed notification-config `PUT` candidate, signed super-user `POST /digest/dispatch` and `/config/test-webhook` candidates, and signed super-user email test/redelivery candidates (canonical and `/source` paths) persist idempotent local projections with source-shaped readback, replay/audit evidence, and tombstones/overlays; the trusted gateway now forwards read-all, digest dispatch, email test, and email redelivery, and Rabbita `/inbox`/`/notify-config` expose the actions, with gateway smoke proving authenticated replay equality, recipient redaction, and the missing-imported-row boundary. Digest receipts summarize imported warning/recipient evidence, webhook-test receipts report effective URL/kind state, and email candidates verify recipient/outbox evidence without inserting/updating `sys_email_outbox`, enqueuing delivery, or calling a provider. Configuration candidates redact secret values and never bind credentials. Imported `sys_warning_subscription`, `sys_message`, and `sys_email_outbox` rows remain read-only, and all commands explicitly have no delivery, provider, authority, accounting, cash, or tax effect. The pure-shell smoke covers create/replay/list/update/delete, message read/read-all, config, digest, webhook-test, email-test, and redelivery candidates/replays/readback, source aliases, invalid channels, secret redaction, source-row immutability, and PostgreSQL cleanup. The controlled export has no notification source rows and five imported users, so Rabbita `/inbox` and `/notify-config` render explicit empty-source states after successful reads; managed credentials, delivery, consent, production identity, and owner acceptance remain separate gates. Python is frozen comparison evidence only. |
| ERP OCR/error-log runtime reads | `cmd/postgres_company_service/observations.mbt` + `scripts/company_postgres_source_read_smoke.sh` + `frontend/main` + `ERP_ADMIN_RUNTIME_AUDIT.md` | Native MoonBit now owns `/api/company/admin/ocr/status` and `/api/company/admin/error-log`, preserving source-backed provider/scene selection, redacted key-status counts, provider definitions, bounded error rows with keyword filtering and 5xx/today counters, and no OCR/provider execution. The controlled export has no `sys_param` or `sys_error_log` rows, so Rabbita `/ocr-config` and `/error-log` show explicit empty/definition states. OCR calls, config writes, error retention, production identity, and super-user owner acceptance remain separate gates. |
| ERP OCR/LLM provider-test candidates | `cmd/postgres_company_service/ai_provider_commands.mbt` + `cmd/postgres_company_gateway` + `scripts/company_postgres_ai_provider_smoke.sh` + `scripts/company_postgres_ai_provider_gateway_smoke.sh` + `docs/ERP_ADMIN_RUNTIME_AUDIT.md` + `frontend/main` | Native MoonBit now serves signed super-user `/api/company/admin/ocr/test`, `/api/company/admin/llm/test`, and notification `/llm-test` candidates with provider/model/key/endpoint digest evidence, replay/audit receipts, `dryRun=true`, and `providerExecution=false`. The native gateway forwards canonical/source OCR and LLM aliases, and Rabbita `/ocr-config` exposes all three bounded provider-test controls with replay/redaction status. No OCR/LLM provider call or credential value is exposed; production execution, identity, and security-owner acceptance remain separate gates. |
| ERP AI analytics runtime reads and badge batch | `cmd/postgres_company_service/observations.mbt` + `cmd/postgres_company_service/main.mbt` + `scripts/company_postgres_ai_stats_smoke.sh` + `scripts/company_postgres_source_read_smoke.sh` + `frontend/main` + `ERP_AI_RUNTIME_AUDIT.md` | Native MoonBit now serves source-compatible overview/activity/badge plus `POST /api/company/ai-stats/badge/batch` (and `/source`) from imported PostgreSQL evidence. Batch results select confirmed `ai_draft` rows by `bizType`/bounded `bizGuids`, preserve the source map shape, and report `authorizing=false`, `persisted=false`, and `provider_execution=false`; no AI provider or draft mutation occurs. The controlled export has no AI analytics rows, so Rabbita `/ai-stats` renders explicit empty-source state after successful reads; LLM/OCR execution, intake/confirmation, prompt retention, workflow authority, production identity, and AI-owner acceptance remain separate gates. |
| ERP AI Hub observation reads, explanation, and command-gate candidates | `cmd/postgres_company_service/ai_hub_commands.mbt` + `cmd/postgres_company_service/ai_hub_command_commands.mbt` + `scripts/company_postgres_ai_hub_smoke.sh` + `scripts/company_postgres_gateway_smoke.sh` + `scripts/company_postgres_service.py` + `scripts/company_postgres_read_model_server.py` + `scripts/company_postgres_service_smoke.py` + `frontend/main` + `ERP_AI_RUNTIME_AUDIT.md` | Source-compatible usage, draft, query-log, corrections, and correction-stats reads preserve the source response families with coverage and redacted payloads. Signed `POST /api/company/ai-hub/explain` returns a deterministic table summary candidate; Rabbita `/ai-hub` now exposes it from the designer card and the trusted gateway smoke proves forwarding. Rabbita also exposes all nine native command candidates (intake, confirm, discard, query, rule-from-NL, approval-draft, global-ask, query-session, and command) through the trusted gateway. Each returns the signed AI-owner authorization gate without consuming the body, invoking a provider, executing a query, persisting a draft/session/rule, changing workflow authority, or causing cash/accounting/tax effects. The controlled export has no AI Hub draft/query/correction/session rows or AI confirmation audit rows, so Rabbita `/ai-hub` shows successful empty-source metrics/history while retaining the designer workbench. Provider-backed explanation, prompt retention, durable command mutation, draft promotion, production identity, and AI-owner acceptance remain gated. |
| ERP webhook configuration, test-delivery candidate, overdue-scan candidate, and preview runtime | `cmd/postgres_company_service/webhook_commands.mbt` + `cmd/postgres_company_service/webhook_delivery_commands.mbt` + `cmd/postgres_company_service/webhook_scan_commands.mbt` + `cmd/postgres_company_service/main.mbt` + `cmd/postgres_company_gateway` + `scripts/company_postgres_webhook_smoke.sh` + `scripts/company_postgres_webhook_gateway_smoke.sh` + `scripts/company_postgres_source_read_smoke.sh` + `frontend/main` + `ERP_WEBHOOK_RUNTIME_AUDIT.md` | Native MoonBit now owns `/api/company/webhook/config` plus the `/source` read alias, preserving the three-platform `sys_param` shape with redacted URL/secret status and explicit non-authorizing/provider-off metadata. Signed super-user `PUT /config/:platform` candidates preserve enabled/optional URL/secret/`__keep__` semantics, persist URL/secret digests with idempotent command and audit projections, and never bind credentials or deliver. Signed `POST /test/:platform` candidates validate effective config state and persist redacted dry-run receipts with `wouldSend`/skip reasons; canonical/source test aliases are now gateway-forwarded and Rabbita `/webhook-config` exposes a bounded dry-run control. Rabbita now also exposes the signed overdue-scan preview and receipt candidates; they reuse imported overdue tickets, persist only the bounded receipt for the command candidate, and expose `dryRun=true`, `sent=false`, and `ticketMutation=false`, while provider execution remains false. Dry-run `POST /scan-overdue/preview` (canonical and `/source`) remains the read counterpart. The controlled export has no `sys_param` or warning-ticket rows, so Rabbita `/webhook-config` renders empty-source metadata; managed credential storage, actual test delivery, production overdue mutation, production identity, and notification-owner acceptance remain separate gates. The pure-shell webhook smokes cover replay, update, test/scan candidates and replays, aliases, redaction, invalid-platform rejection, preview, forwarding, and cleanup. |
| ERP report-builder runtime and local commands | `cmd/postgres_company_service/report_commands.mbt` + `cmd/postgres_company_service/report_share_commands.mbt` + `cmd/postgres_company_service/report_export.mbt` + `cmd/postgres_company_service/observations.mbt` + `scripts/company_postgres_report_template_smoke.sh` + `scripts/company_postgres_report_share_smoke.sh` + `scripts/company_postgres_report_export_smoke.sh` + `scripts/company_postgres_source_read_smoke.sh` + `frontend/main` + `ERP_REPORT_BUILDER_RUNTIME_AUDIT.md` | Native MoonBit now preserves ten table definitions and eight operators with explicit `sys_report_template` coverage, merges imported and command-owned templates, and supports signed create/delete commands with immutable revisions, replay receipts, audit events, owner-only deletion, and imported-row protection. Template run validates the allow-list and evaluates imported JSON envelopes in memory (`sql_executed=false`); it never accepts raw SQL or changes provider, cash, accounting, or tax state. Report-share create/list/revoke and public metadata/data reads now use a command-owned revision overlay, bounded five-report allow-list, forwarded-TLS gate, and access-count revisions. Authenticated `POST /api/company/export/excel` emits bounded source-compatible XLSX without Python, SQL execution, provider calls, or financial effects. The pure-shell smokes cover metadata, template/share/export replay and readback, ZIP validity, filtered/public reads, revoke, and tombstone behavior. PDF export, managed public serving, production identity, browser acceptance, and report-owner acceptance remain separate gates. Python is frozen comparison evidence only. |
| ERP cashflow runtime read and explanation candidate | `cmd/postgres_company_service/cashflow.mbt` + `cmd/postgres_company_service/ai_explain_commands.mbt` + `scripts/company_postgres_service.sh` + `scripts/company_postgres_ai_explain_smoke.sh` + `scripts/company_postgres_gateway_smoke.sh` + `frontend/main` + `ERP_CASHFLOW_RUNTIME_AUDIT.md` | Native MoonBit now owns source-compatible `/api/company/cashflow/forecast`, `/forecast-v3`, `/forecast/detail`, `/inflow`, `/net`, and `/gap-alert`, preserving source payment-plan/application, imported expense/loan contributions, top-five business-unit/project rollups, source revenue inflow/overdue buckets, CBS A/D/E/FG forecast totals, weekly gap buckets, and net cash series with explicit coverage and `authorizing=false`. Signed `/cashflow/ai-explain` plus `/source` is a deterministic analytics candidate over supplied series/gaps; Rabbita `/cashflow` now exposes and renders the returned gap summary, while the gateway smoke proves browser forwarding. Cash release, real provider execution, bank settlement, browser identity, and finance-owner acceptance remain separate gates. |
| ERP profile runtime read and preference command | `cmd/postgres_company_service` + `cmd/postgres_company_service/preferences.mbt` + `scripts/company_postgres_service.sh` + `scripts/company_postgres_preference_smoke.sh` + `scripts/company_postgres_service.py` + `scripts/company_postgres_read_model_server.py` + `scripts/company_postgres_service_smoke.py` + `scripts/company_postgres_dev_gateway.py` + `scripts/company_postgres_dev_gateway_smoke.py` + `frontend/main` + `ERP_PROFILE_RUNTIME_AUDIT.md` | Native MoonBit authenticated service now shadow-matches the Python bridge for `/api/company/auth/me`, `/api/company/auth/prefs`, and `/api/company/auth/my-initiated` on the live PostgreSQL target. Signed `PUT`/`DELETE /api/company/source/auth/prefs/:key` commands bind to the signed enabled user, persist idempotent local revisions and audit receipts, merge active values on readback, tombstone deletes, and expose explicit non-authorizing/provider/accounting/cash/tax-neutral markers; the preference smoke proves set/replay/read/delete without Python. Password/profile writes, production identity, browser acceptance, and owner approval remain separate gates. |
| ERP admin governance/backup runtime reads and dictionary/sys-param command candidates | `cmd/postgres_company_service/admin_commands.mbt` + `cmd/postgres_company_service/admin_dictionary_commands.mbt` + `cmd/postgres_company_gateway` + `scripts/company_postgres_admin_sys_param_smoke.sh` + `scripts/company_postgres_admin_sys_param_gateway_smoke.sh` + `scripts/company_postgres_ai_provider_gateway_smoke.sh` + `scripts/company_postgres_admin_dictionary_smoke.sh` + `scripts/company_postgres_service.py` + `scripts/company_postgres_dev_gateway.py` + `frontend/main` + `ERP_ADMIN_RUNTIME_AUDIT.md` | Source-compatible admin reads preserve one dictionary group with five options, twelve quality rules (eight evaluated, four explicitly unavailable), imported login audit rows/action aggregates, source-backed audit pagination/filtering, 29 health-table coverage rows, and an explicitly empty BPM pool. Rabbita `/admin` loads dictionary and quality panels and now exposes the PostgreSQL backup boundary; `/system-health` loads table/BPM coverage, `/users` loads the five imported identities, and `/audit-log` loads imported audit rows/action aggregates through the read-only adapter. Native source-shaped dictionary POST/PATCH and generic `sys_param` POST require an enabled imported super-user and persist only redacted command-owned overlays, receipts, and audit events; the dictionary smoke now proves create/update/read, canonical and source aliases, replay, immutable projections, and normal/sensitive value handling. The native gateway now allow-lists dictionary, generic `sys_param`, and canonical/source OCR/LLM test aliases; dedicated gateway smokes prove body-idempotent super-user forwarding and secret-redacted provider dry-runs. Rabbita exposes bounded create/update controls for command-owned dictionary overlays while keeping imported options visibly read-only; browser production-identity acceptance remains open. Imported options/parameters remain read-only and provider, cash, accounting, and tax effects remain false. `GET /api/company/admin/backup/db` is now surfaced as a gated PostgreSQL target-format boundary without MySQL `mysqldump` or binary export; no backup bytes are emitted. Role/permission panels, backup/restore operations, and audit retention remain unavailable source evidence. Production identity, super-user scope, full audit retention, source completeness, and owner acceptance remain separate gates. |
| ERP employee-loan runtime | `cmd/postgres_company_service` + `scripts/company_postgres_loan_smoke.sh` + `scripts/company_postgres_source_read_smoke.sh` + `scripts/company_postgres_gateway_smoke.sh` + `frontend/main` + `ERP_LOAN_RUNTIME_AUDIT.md` | Compiled MoonBit serves source loan list/detail and offset evidence from PostgreSQL with source identity, applicant/department/project labels, balances, and read-only provenance; Rabbita `/loans` shows the live rows with fixture fallback. Local create/submit/bounded offset/draft update/draft-rejected void commands persist authority evidence, idempotent receipts, immutable revisions, and audit events through the authenticated service/gateway; shell smoke proves the native path without Python. Workflow synchronization remains gated because the available export has no `wf_process_instance` rows; browser production identity and owner acceptance remain pending. |
| Authenticated bounded PostgreSQL company service (local) | `cmd/postgres_company_service` + `scripts/company_postgres_service.sh` + `cmd/postgres_company_gateway` + `scripts/company_postgres_gateway.sh` + `scripts/company_postgres_gateway_smoke.sh` + `scripts/company_postgres_expense_smoke.sh` + `scripts/company_postgres_contract_smoke.sh` + `scripts/company_postgres_payment_smoke.sh` + `scripts/company_postgres_milestone_smoke.sh` + `scripts/company_postgres_service.py` (frozen bridge) + `ERP_EXPENSE_RUNTIME_VERTICAL.md` + `ERP_CONTRACT_RUNTIME_VERTICAL.md` + `ERP_PAYMENT_APPLICATION_RUNTIME_VERTICAL.md` + `ERP_PROCUREMENT_RUNTIME_VERTICAL.md` + `ERP_SALES_RUNTIME_VERTICAL.md` + `ERP_DELIVERY_RUNTIME_AUDIT.md` + `ERP_PROJECT_RUNTIME_AUDIT.md` + `ERP_LOAN_RUNTIME_AUDIT.md` + `ERP_CASHFLOW_RUNTIME_AUDIT.md` + `ERP_CBS_RUNTIME_AUDIT.md` | Native MoonBit now owns a mandatory bearer-token/forwarded-TLS service slice for health, summary, receipts, projections, imported profile/preferences/initiated-document/source observations, imported expense, contract, payment-application, and contract-milestone list/detail, calculation-only budget preview, and idempotent expense, contract, payment-application, and contract-milestone command lifecycles, plus the native gateway/session boundary. The shell smokes prove native expense/contract/payment/milestone replay and lifecycle behavior, session login/logout, actor signing, trusted identity, and forwarding without Python. The broad Python service remains frozen bridge evidence for routes not yet ported; it is not an accepted build, test, or deployment dependency. |
| Reviewed access/role import boundary | `cmd/access_plan` + `scripts/erp_access_plan.sh` + `cmd/access_import` + `foundation/access` + `ERP_ACCESS_BOUNDARY.md` | Native MoonBit compiles the explicit source role/identity/scope map to a reviewed access plan; the native importer builds local roles, bounded permissions, exact-scope assignments, and separation rules, emits one `access_directory` candidate with `authority_migrated=true`, and passes SQLite/PostgreSQL parity plus zero-insert replay. Native output is JSON-equivalent to the frozen Python planner for empty and synthetic source exports. The fixture is synthetic because `sys_role`/`sys_user_role` are schema-only; authentication secrets and super-user privilege remain excluded. |
| Managed production deployment contract | `docs/PRODUCTION_DEPLOYMENT_GATE.md` + `cmd/production_deployment_check` + `cmd/production_service_check` + native shell wrappers | Native MoonBit deployment/service gates reproduce the former credential-free Python validators: managed PostgreSQL, bounded pooling, TLS, backups, restore, observability, approvals, and fixed service boundaries. The Python implementations remain frozen comparison evidence; provider provisioning and owner approval remain open. |
| Durable native-promotion projections | `cmd/sqlite_projection_apply` + `scripts/company_sqlite_projection_apply.sh` + `scripts/erp_migration_rehearsal.sh` | Native MoonBit now validates promotion receipts, rejects secret-shaped candidates and event conflicts, computes canonical payload/event/revision/hash state, emits a transactional SQLite SQL plan, records the cohort receipt, verifies integrity, and makes an identical replay insert zero rows. Integration behind the managed production service/pool remains pending. |
| Native projection parity gate | `cmd/sqlite_projection_parity` + `scripts/company_sqlite_projection_parity.sh` + `scripts/erp_migration_rehearsal.sh` | Reopened SQLite projections now use the native MoonBit verifier through a read-only shell/SQLite query boundary. It compares exactly with the native receipt by source identity and target type, reports `shadow_verified` with 19/19 items for the mapped cohort, and fails with explicit missing/extra findings for a mismatched cohort. |
| Native PostgreSQL projection parity gate | `cmd/postgres_projection_parity` + `scripts/company_postgres_projection_parity.sh` | Reopens PostgreSQL aggregate projections for one domain-promotion receipt, compares target/source identities and multiplicities, emits the existing parity envelope, and exits non-zero with explicit missing/extra findings on mismatch. It is read-only and uses the PostgreSQL driver directly; the historical Python parity helper remains frozen comparison evidence. |
| Native PostgreSQL accounting-link parity gate | `cmd/postgres_accounting_link_parity` + `scripts/company_postgres_accounting_link_parity.sh` | Reopens PostgreSQL accounting links for one validated receipt, compares event/source/journal/principal identity, emits the existing `shadow_verified`/`mismatch` envelope, and leaves `cash_released=false` and `period_posted=false`. It is read-only; SQLite and broader reconciliation helpers remain separate frozen evidence until ported. |
| Native PostgreSQL accounting reconciliation gate | `cmd/postgres_accounting_reconciliation` + `scripts/company_postgres_accounting_reconciliation.sh` | Validates domain/plan/receipt identity, principal/amount/currency continuity, and durable PostgreSQL event links, then writes the existing reconciled report with `cash_released=false` and `period_posted=false`. It never posts journals or closes periods; SQLite and cross-domain reconciliation remain separate follow-up ports. |
| Native accounting-link planner | `cmd/accounting_link_plan` + `scripts/erp_accounting_link_plan.sh` + `scripts/company_postgres_accounting_cohort_rehearsal.sh` | Replaces the Python planner for the PostgreSQL cohort path. It preserves the allow-listed target/source types, target-specific mapping keys, explicit principal/amount/currency checks, specialized event identity checks, quarantine reasons, and the `moonproj.erp.accounting-link-plan.v1` envelope before the native accounting-link validator runs. |
| Cohort-scoped receipt replay | `cmd/sqlite_projection_apply` + `scripts/company_sqlite_projection_apply.sh` + `scripts/company_sqlite_accounting_link_apply.py` | Projection receipt hashes are scoped to their source snapshot/mapping cohort, so a later offset cohort may append rows without invalidating an earlier receipt; rerunning the complete native projection wrapper inserts zero projections and preserves all receipts. Accounting-link replay remains the next SQLite adapter port. |
| Native accounting-link receipt and durable apply | `scripts/erp_accounting_link_plan.py` + `cmd/accounting_link` + `scripts/company_sqlite_accounting_link_apply.py` | The allow-listed source-to-journal boundary now covers commitment, advance/offset, settlement, expense, delivery progress/recognition, receivable opening/collection, payable opening/payment, tax, financing, investment acquisition/valuation, asset, cash, and bank-statement source types. It requires explicit candidate/mapping principal, amount, and currency equality; investment valuation, tax obligation, and financing facility links additionally require exact native event, journal, account, scope, and event-type identity, while duplicate invoice/tender source rows prefer target-specific mapping keys. The existing commitment/advance receipt persists three links transactionally, while synthetic settlement, receivable, delivery-recognition, tax, financing, valuation, invoice, and procurement smokes prove native receipt generation and missing-amount quarantine. It does not post cash or infer accounting policy. |
| PostgreSQL accounting-link cohort rehearsal | `scripts/company_postgres_accounting_cohort_rehearsal.sh` + `scripts/company_postgres_accounting_link_apply.py` + `scripts/company_investment_valuation_accounting_rehearsal.sh` + `scripts/company_invoice_procurement_accounting_rehearsal.sh` + `scripts/company_tax_financing_accounting_rehearsal.sh` + `scripts/company_expense_advance_accounting_rehearsal.sh` | Runs reviewed domain receipts through the allow-listed planner, native validator, PostgreSQL traceability apply, exact identity parity, and idempotent replay. The dedicated valuation rehearsal appends one link, invoice/procurement appends four, tax/financing appends four, and employee advance/offset appends two; all are outside the aggregate projection count and do not post cash or journals. The main typed rehearsal remains at 7 links unless these separate accounting cohorts are supplied. |
| Settlement-request accounting-link cohort | `scripts/fixtures/payment_accounting_link_mapping.json` + `scripts/erp_accounting_link_plan.py` + `scripts/erp_migration_rehearsal.sh` | A separately scoped seventh-argument map validates 3 requested-settlement/payment-application journals against the typed payment receipt, persists 3 additional links transactionally (7 total in the extended run), reconciles source/principal/amount/currency, and keeps cash release and period posting false. |
| Reconciled period-close control | `finance/accounting` + `finance/reconciliation` + `cmd/period_close_control` + `scripts/company_period_close_control.sh` | Native `AccountingBook.close_reconciled` refuses to close without balanced reports; the native evidence command locks all cohorts to one source snapshot, deduplicates identical SQLite/PostgreSQL backend evidence by source snapshot/mapping identity, records mapping versions and a deterministic evidence hash, and emits a `ready_for_reconciled_close` artifact while keeping `close_authorized=false`, cash release false, and period posting false. |
| SQLite driver transaction smoke | `cmd/sqlite_driver_smoke` + `scripts/company_sqlite_driver_smoke.sh` | Native MoonBit emits the shared SQLite schema and transaction probes; the shell verifies foreign keys/WAL settings, intentional rollback, duplicate rejection with transaction rollback, one committed record, and `PRAGMA integrity_check`, then emits the historical `driver_transaction_verified` envelope and removes the ephemeral database. The Python driver smoke remains frozen comparison evidence. |
| Accounting/subledger reconciliation evidence | `scripts/company_sqlite_accounting_reconciliation.py` + `scripts/company_postgres_accounting_reconciliation.py` + `scripts/company_accounting_link_parity.py` + `scripts/erp_migration_rehearsal.sh` + `ERP_SETTLEMENT_ACCOUNTING.md` | Reconciles base, employee advance/offset, synthetic receivable-collection/payable-payment, invoice-opening, payable-opening, procurement-commitment, and valuation links back to promoted source candidates, principal, amount, currency, and durable event/source/journal rows; dedicated cohorts now emit the same source-to-durable reconciliation shape on SQLite and PostgreSQL. Reports remain traceability-only with `cash_released=false` and `period_posted=false`. |
| SQLite backup/restore parity gate | `cmd/sqlite_backup_restore` + `scripts/company_sqlite_backup_restore.sh` + `scripts/erp_migration_rehearsal.sh` | Native MoonBit and the shell back up the final rehearsal database, reopen the restored file, verify integrity and schema version, and compare deterministic logical digests and counts for raw records, aggregate projections, accounting links, receipts, and schema rows. The Python implementation remains frozen comparison evidence. |
| ERP row-coverage evidence | `cmd/row_coverage` + `scripts/erp_row_coverage.sh` + `scripts/erp_migration_rehearsal.sh` | Native MoonBit validates snapshot/full-export manifests, primary-key identity and uniqueness, direct accepted receipt identities, and workflow/investment/parameter nested structural counts; the shell supplies only the direct JSON evidence paths. With the reviewed base, typed, and advance-offset maps, the authoritative export reaches 120/120 covered rows while promotion and cutover authorization remain false. |
| Business-acceptance decision gate | `cmd/business_acceptance_check` + `scripts/company_business_acceptance_check.sh` + `scripts/erp_migration_rehearsal.sh` | Native MoonBit validates the credential-free owner packet, source snapshot hash, five required decision IDs/roles, decision evidence fields and allow-list, then emits the exact pending/authorized result. The example pending packet is oracle-equivalent; shadow and cutover remain explicitly unauthorized. |
| Read-only shadow-period gate | `cmd/shadow_period_check` + `scripts/company_shadow_period_check.sh` + `scripts/erp_migration_rehearsal.sh` | Native MoonBit validates the legacy-authoritative/read-only contract, bounded period/comparison settings, parity and row-coverage evidence, side-effect-free reconciliation reports, and the explicit shadow owner decision; it emits `shadow_ready` or `shadow_pending_owner` while keeping target mutations and cutover authorization false. |
| Managed-production deployment gate | `cmd/production_deployment_check` + `scripts/company_production_deployment_check.sh` + `scripts/erp_migration_rehearsal.sh` | Native MoonBit validates the PostgreSQL production manifest, credential-free DSN environment reference, TLS/certificate verification, bounded pool, encrypted cross-region backup/restore, operational observability, and structured finance/operations/security approvals; the example remains `ready_for_owner_review`. |
| Managed-production service gate | `cmd/production_service_check` + `scripts/company_production_service_check.sh` + `scripts/erp_migration_rehearsal.sh` | Native MoonBit validates the private authenticated HTTPS service boundary, bounded connection/request limits, schema-matched readiness, explicit HTTPS CORS origins, fixed read-model endpoint allow-list, empty mutation surface, and no arbitrary SQL; it remains `ready_for_service_review` until the deployment gate is authorized. |
| Cutover evidence aggregator | `cmd/cutover_gate` + `scripts/company_migration_cutover_gate.sh` + `scripts/erp_migration_rehearsal.sh` | Native MoonBit evaluates the fail-closed staging/schema/mapping/relationship/route/database/backup/driver/production/coverage/acceptance/shadow/parity/exception/replay/reconciliation checks and emits the existing cutover-gate envelope. The shell supplies only SQLite integrity/count facts; the full reviewed rehearsal is JSON-equivalent to the Python oracle, reaches `ready_for_business_acceptance`, and leaves `cutover_authorized=false`. |
| Cutover readiness evidence gate | `cmd/cutover_gate` + `scripts/company_migration_cutover_gate.sh` + `scripts/erp_migration_rehearsal.sh` | Native MoonBit combines source staging, schema scope, target counts, all cohort parity reports, replay evidence, SQL-driver smoke, backup/restore, period-close snapshot/evidence-hash checks, and task-state exception evidence into a fail-closed decision artifact. The Python implementation remains frozen comparison evidence; `cutover_authorized=false` and schema/owner exceptions remain explicit. |
| ERP schema-scope evidence gate | `cmd/schema_gap` + `scripts/erp_schema_gap_report.sh` + `cmd/cutover_gate` | MoonBit compares the authoritative `erp_new` initializer with the exported fixture (75 schema definitions / 26 present / 49 schema-only), assigns every table a baseline capability ID and migration action, and carries the scope gap into the native cutover artifact as an explicit open exception; it never treats the fixture as full ERP coverage. |
| Runtime source-reconciliation inventory | `cmd/schema_gap` + `cmd/postgres_company_service/migration.mbt` + `scripts/erp_schema_gap_report.sh` + `scripts/company_postgres_service.py` + `scripts/company_postgres_read_model_server.py` + `scripts/company_postgres_service_smoke.py` + `frontend/main` | MoonBit `cmd/schema_gap` and the native PostgreSQL service now produce the hash-bound 75-table schema gap from the export manifest; the shell rehearsal invokes it without Python. `/api/company/source/migration/schema-coverage` exposes the same 26-table snapshot, 49 schema-only tables, per-table raw-envelope counts, and non-authorizing `source_export_incomplete` state; Rabbita System Health renders the migration-scope evidence. It measures the gap without fabricating rows; promotion, cutover, and owner acceptance remain false. |
| Credential-free full-export contract | `cmd/export_contract` + `scripts/erp_export_contract.sh` + `docs/ERP_FULL_EXPORT_CONTRACT.md` | MoonBit verifies per-table hashes, row counts, safe paths, non-null/unique primary-key identity for every exported non-empty row, recursive secret rejection, and 75-table coverage before staging; the current snapshot is correctly reported as `source_export_incomplete` with 49 missing tables. The former Python checker is bridge-only and no longer runs in the rehearsal path. |
| Owner-approved empty-table disposition contract | `cmd/empty_disposition` + `scripts/erp_empty_disposition.sh` + `docs/ERP_EMPTY_TABLE_DISPOSITION.md` | Native MoonBit generates and validates all 49 schema-only entries, requiring zero-row evidence, the exact empty-table hash, a source snapshot hash, named owner, UTC approval time, rationale, and evidence reference for every `owner_approved_empty` entry; the current template remains `awaiting_owner_disposition` and never authorizes promotion or cutover. The former Python checker is frozen comparison evidence only. |
| ERP source-export request | `cmd/source_export_request` + `scripts/erp_source_export_request.sh` + `docs/ERP_SOURCE_EXPORT_REQUEST.md` | MoonBit generates an exact read-only, credential-free request for all 49 absent tables, including wave, capability, primary-key, hash, and redaction requirements; the shell wrapper is the supported entry point and the artifact remains `awaiting_source_export`. |
| ERP MySQL metadata probe | `scripts/erp_mysql_inventory.mjs` + `docs/ERP_MYSQL_SOURCE_PROBE.md` | Uses the configured ERP source only for table/row-count/primary-key metadata, never payloads or credentials; the configured endpoint currently returns `ECONNREFUSED`, so the SQLite snapshot remains authoritative. |
| Migration completion audit | `docs/MIGRATION_COMPLETION_AUDIT.md` | Separates repository-verified requirements from the incomplete source export, provider approvals, business acceptance, shadow operation, and ownership-transfer gates. |
| Source row-coverage ledger | `cmd/row_coverage` + `scripts/erp_row_coverage.sh` + `docs/ERP_ROW_COVERAGE.md` | Native MoonBit reads source identities and all native promotion/evidence receipts, accounts for aggregated workflow/investment/parameter rows, and verifies available rows have disposition coverage without authorizing promotion. The Python implementation remains comparison evidence. |
| Business-acceptance packet | `cmd/business_acceptance_check` + `scripts/company_business_acceptance_check.sh` + `docs/BUSINESS_ACCEPTANCE_PACKET.md` | Native MoonBit validates five named business/finance/operations/migration-owner decisions against the source snapshot and technical evidence; the example remains `acceptance_pending` and cannot authorize cutover. |
| Read-only shadow-period contract | `cmd/shadow_period_check` + `scripts/company_shadow_period_check.sh` + `docs/SHADOW_PERIOD_CONTRACT.md` | Native MoonBit binds legacy authority, disabled target mutations, comparison dimensions, duration, parity, row coverage, accounting no-effect, and rollback evidence; the example remains `shadow_pending_owner`. |
| ERP schema-only cohort plan | `cmd/schema_cohort_plan` + `scripts/erp_schema_cohort_plan.sh` + `docs/ERP_SCHEMA_COHORTS.md` | MoonBit orders all 49 absent tables into seven capability/dependency waves with explicit credential, token, attachment, email, and retention actions; the shell rehearsal and cutover gate verify the plan while keeping authorization false. The former Python planner is bridge-only. |
| ERP foundation-security schema mapping | `cmd/schema_cohort_mapping` + `scripts/erp_schema_cohort_mapping.sh` + `docs/ERP_FOUNDATION_SECURITY_COHORT.md` | MoonBit maps all six first-wave tables to evidence, parameters, identity security, RBAC, and preference boundaries; the current snapshot has zero rows, so the artifact is `mapped_scope_only` and cannot authorize promotion. |
| ERP workflow-control schema mapping | `cmd/schema_cohort_mapping` + `scripts/erp_schema_cohort_mapping.sh` + `docs/ERP_WORKFLOW_CONTROL_SCHEMA_COHORT.md` | MoonBit maps seven approval/runtime-assignee/warning tables to workflow and intelligence boundaries; custom SQL is not executed implicitly, and zero available source rows keep the artifact non-promotable. |
| ERP cost-investment schema mapping | `cmd/schema_cohort_mapping` + `scripts/erp_schema_cohort_mapping.sh` + `docs/ERP_COST_INVESTMENT_SCHEMA_COHORT.md` | MoonBit maps nine CBS and investment-model tables to governed configuration/evidence boundaries; no budget, accounting, formula, or investment effect is inferred, and zero source rows keep the artifact non-promotable. |
| ERP procurement-contract schema mapping | `cmd/schema_cohort_mapping` + `scripts/erp_schema_cohort_mapping.sh` + `docs/ERP_PROCUREMENT_CONTRACT_SCHEMA_COHORT.md` | MoonBit maps seven supplier/tender/contract/milestone tables to procurement and contract boundaries; awards do not create commitments or payments, and zero source rows keep the artifact non-promotable. |
| ERP sales-receivables schema mapping | `cmd/schema_cohort_mapping` + `scripts/erp_schema_cohort_mapping.sh` + `docs/ERP_SALES_RECEIVABLES_SCHEMA_COHORT.md` | MoonBit maps eight customer/invoice/refund/revenue tables to sales, payable, receivable, and tax boundaries; collection, revenue, tax, and cash remain separate, and zero source rows keep the artifact non-promotable. |
| ERP delivery-treasury schema mapping | `cmd/schema_cohort_mapping` + `scripts/erp_schema_cohort_mapping.sh` + `docs/ERP_DELIVERY_TREASURY_SCHEMA_COHORT.md` | MoonBit maps eight delivery/treasury/marketing tables; evidence, acceptance, planning, dispatch, and spend remain separate, and zero source rows keep the artifact non-promotable. |
| ERP reporting-notification schema mapping | `cmd/schema_cohort_mapping` + `scripts/erp_schema_cohort_mapping.sh` + `docs/ERP_REPORTING_NOTIFICATION_SCHEMA_COHORT.md` | MoonBit maps four message/email/report/share tables; tokens are excluded, templates are allow-listed, and zero source rows keep the artifact non-promotable. |
| ERP relationship/orphan audit gate | `cmd/relationship_audit` + `scripts/erp_relationship_audit.sh` + `scripts/erp_migration_rehearsal.sh` + `cmd/cutover_gate` | Native MoonBit executes the 60 reviewed source relationships across the 26-table fixture, checks 216 non-empty references, and currently finds zero orphans. The report is source-side evidence and does not imply target ownership. |
| ERP route-surface inventory gate | `cmd/route_inventory` + `scripts/erp_route_inventory.sh` + `scripts/erp_ui_parity_matrix.py` + `scripts/erp_migration_rehearsal.sh` + `cmd/cutover_gate` | MoonBit parses the actual ERP route directory into capability-tagged handler records: 30 route files, 338 `r.get/post/put/patch/delete` handlers, and 28 `r.use` middleware registrations. The UI parity matrix remains a separate comparison artifact; the route evidence is carried into the native cutover gate and remains `functional_parity_incomplete` until broader workflows are accepted. |
| End-to-end ERP rehearsal | `scripts/erp_migration_rehearsal.sh` | Runs backup extraction, redacted export, raw staging, and durable SQLite apply as one repeatable read-only-source command; a second run inserts zero rows. |
| Mapped-cohort promotion plan | `scripts/erp_promotion_plan.py` + `docs/ERP_PROMOTION_MAPPING.md` | Reads the real sanitized export, applies explicit principal/counterparty/employee/currency/cost mappings and fixed-point policy, emits 19 candidate items, and quarantines unresolved mappings instead of guessing. |
| Mapped-cohort domain promotion | `cmd/promote` + `migration/erp` | Native MoonBit command refuses quarantined plans and applies the 19 ready candidates through business-unit, project, contract, cost, and advance domain importers, emitting a source/mapping/count receipt. |
| Typed workflow-definition promotion | `scripts/erp_workflow_promotion_plan.sh` + `cmd/workflow_promotion_plan` + `cmd/promote` + `migration/erp` | The workflow compiler is now native MoonBit and preserves explicit capability mappings, ordered-step checks, deterministic quarantine reasons, and the exact workflow-promotion envelope. Actual export rows produce 2 ready process definitions and 12 ordered steps; a missing step mapping quarantines its process before domain import. |
| Typed project-lifecycle promotion | `scripts/erp_lifecycle_promotion_plan.sh` + `cmd/lifecycle_promotion_plan` + `cmd/promote` + `migration/erp` | The lifecycle compiler is now native MoonBit and preserves principal/stage mappings, half-even progress normalization, deterministic ordering, and the exact lifecycle-promotion envelope. Actual export rows produce 2 ready project masters and 2 lifecycle cohorts; explicit stage mappings replay `proj-0001` to development and `proj-0002` to design under project-scoped authority. Missing stage mappings quarantine before domain import. |
| Typed project-task structure promotion | `scripts/erp_task_promotion_plan.sh` + `cmd/task_promotion_plan` + `cmd/promote` + `migration/erp` | The task-structure compiler is now native MoonBit and preserves dependency identity/order, half-even progress normalization, deterministic quarantine reasons, and the exact task-promotion envelope. Actual export rows produce 2 ready project task plans containing 7 and 2 dependency-ordered tasks. Structure is promoted under `project:task:add`; source state/progress remains evidence because the fixture's child-state history conflicts with target dependency replay. |
| Typed cohort durable projection/parity runner | `scripts/erp_typed_cohort_rehearsal.sh` + `scripts/erp_mapping_variant.sh` + `cmd/task_state_evidence` | The mapping-variant step is now native MoonBit. Eight separately versioned business cohorts plus the clean project-2 task-state cohort, one quarantined task-state observation, a source-bound investment evaluation, and a typed-evidence cohort promote 76 accepted typed items into SQLite projections; every reopened cohort reports exact `shadow_verified` parity, and a second run inserts zero projections. |
| Typed evidence preservation cohort | `scripts/erp_typed_evidence_promotion_plan.sh` + `cmd/typed_evidence_promotion_plan` + `cmd/promote` + `persistence/store` | The typed-evidence compiler is now native MoonBit and preserves the six-table allow-list, recursive secret-shaped-key quarantine, source identity and raw numeric representation, deterministic reasons, and the exact typed-evidence envelope. Nine task snapshots, one task report, six workflow assignees, fourteen lifecycle-instance history rows, seven lifecycle catalog rows, and three proceeding rows are preserved as 40 evidence-only projections; no authority, workflow, or economic state is inferred. |
| Typed investment-model promotion and evaluation | `scripts/erp_investment_promotion_plan.sh` + `cmd/investment_promotion_plan` + `cmd/promote` + `cmd/investment_model_eval` + `investment/model` | The investment planner is now native MoonBit and preserves principal/version/index authority, deterministic index ordering, source value representations, stray-index quarantine, and the exact promotion envelope. Actual export rows produce 1 ready investment model with 26 indexes; the follow-on evaluator classifies numeric/date/source values, checks three parent totals, derives four explicit ratio metrics, and persists a source-bound analytics-only projection without execution, position, accounting, or cash effects. Unknown formula semantics remain preserved evidence. |
| Typed commitment-state/payment promotion | `scripts/erp_payment_promotion_plan.sh` + `cmd/payment_promotion_plan` + `cmd/promote` + `migration/erp` | The payment compiler is now native MoonBit and preserves explicit contract-state maps, half-even monetary normalization, deterministic milestone ordering, commitment bounds, and the exact payment-promotion envelope. The fixture replays 2 commitments through performed, turns 4 payment-plan rows into planned milestones, and turns 3 applications into requested settlements; approval, cash release, reconciliation, and accounting remain separate target events. |
| Credential-free user promotion | `scripts/erp_user_promotion_plan.sh` + `cmd/user_promotion_plan` + `cmd/promote` + `migration/erp` + `foundation` | The user compiler is now native MoonBit and preserves principal/business-unit/department assignment, enabled state, deterministic quarantine reasons, and the exact user-promotion envelope. Actual export rows promote 5 user identities. Passwords, network data, authentication timestamps, and legacy super-user privilege remain excluded or evidence-only. |
| Explicit audit-record promotion | `scripts/erp_audit_promotion_plan.sh` + `cmd/audit_promotion_plan` + `cmd/promote` + `migration/erp` + `foundation/evidence` | The audit compiler is now native MoonBit and preserves explicit target/outcome mappings, actor-scoped identity, deterministic quarantine reasons, and the exact audit-promotion envelope. Actual export rows promote 2 audit records only; missing mappings quarantine, and redacted network fields remain excluded. |
| Parameter/dictionary promotion | `scripts/erp_parameter_promotion_plan.sh` + `cmd/parameter_promotion_plan` + `cmd/promote` + `migration/erp` + `foundation` | The parameter compiler is now native MoonBit and preserves explicit principal/scope grants, source-table allow-listing, deterministic option order, duplicate/value quarantine, and the exact parameter-promotion envelope. Actual export rows promote 2 opaque dictionaries with 8 options: 5 `cost_subject` options plus 3 `expense_proceeding` options from `vys_proceeding`; no CBS, accounting, tax, expense-state, or authority meaning is inferred. |
| Task-state/progress exception gate | `scripts/erp_task_state_promotion_plan.sh` + `cmd/task_state_promotion_plan` + `scripts/erp_task_state_exception_review.sh` + `cmd/task_state_exception_review` + `cmd/promote` + `migration/erp` | Native MoonBit now preserves the Python task-state contract: export/project joins, half-even progress normalization, deterministic ordering, dependency/status quarantine reasons, and the review-only exception envelope. The target dependency simulation quarantines 2 inconsistent child states for `proj-0001`; the clean `proj-0002` cohort replays 2 states natively. A review-only artifact records exact conflicts and requires a named decision; no source state is coerced around a missing dependency. |
| Task-state observed-evidence cohort | `scripts/company_task_state_exception_evidence_rehearsal.sh` + `cmd/task_state_evidence` + `ERP_TASK_STATE_OBSERVED_EVIDENCE.md` | The undecided `proj-0001` exception is preserved as one non-authorizing `project_task_state_observation` projection with observed rows and dependency conflicts. SQLite/PostgreSQL parity and zero-insert replay pass; target task state, authority, cash, accounting, and cutover remain unchanged. |
| ERP delivery-progress draft cohort | `scripts/erp_delivery_progress_plan.sh` + `cmd/delivery_progress_plan` + `cmd/delivery_progress` + `operations/delivery` + `persistence/store` | The export-backed compiler is now native MoonBit and preserves the Python contract: task/project joins, explicit project/principal/scope/currency/evidence/value mapping, progress percentage half-even normalization, deterministic quarantine reasons, and the `moonproj.erp.delivery-progress-promotion-plan.v1` envelope. The fixture's `jd_task_report` row can become one explicitly mapped `Draft` progress report with evidence and zero completed value; acceptance, recognition, cost/budget effects, and task-state mutation remain false and exact parity/replay are supported. |
| ERP reviewed delivery-recognition cohort | `scripts/erp_delivery_recognition_plan.sh` + `cmd/delivery_recognition_plan` + `cmd/delivery_recognition` + `operations/delivery` + `persistence/store` | The export-backed compiler is now native MoonBit and preserves the reviewed acceptance contract: task/project joins, accepted review state, reviewed-measurement amount basis, positive measured value, progress and acceptance evidence, explicit accounts, deterministic quarantine reasons, and the `moonproj.erp.delivery-recognition-promotion-plan.v1` envelope. Native promotion emits a `pending_posting` `delivery_recognition` projection with `posted=false`, `cash_released=false`, and `period_closed=false`; no available source row is promoted through this gate yet. |
| ERP source warning cohort | `scripts/erp_warning_plan.sh` + `cmd/warning_plan` + `cmd/warning` + `intelligence/warning` + `persistence/store` | The export-backed compiler is now native MoonBit and preserves explicit project/scope/principal mapping, fixed-point half-even cost scanning, exact positive-overrun source coverage, deterministic evidence, and the `moonproj.company.warning-plan.v1` envelope. Native promotion remains an open warning finding with no notification, workflow, cash, accounting, or tax mutation. |
| ERP fixture importer and migration envelopes | `migration/erp` | Representative project, contract, cost, supplier, tender, milestone, invoice, expense, and employee-loan row translation; typed task/progress, payment, workflow, lifecycle, investment-index, parameter, proceeding, user/audit, and loan-offset envelopes; explicit project-plan and workflow-domain importers; schema-bound JSON envelopes; and raw-row staging for unmapped tables. |
| ERP snapshot inventory | `migration/erp` | Metadata-only inventory of the available 26-table/120-row SQLite backup, with mapped, typed-staged, planned, and empty dispositions and a full-table executable shadow plan. |
| Credential-safe ERP row export | `scripts/erp_snapshot_export.sh` | Deterministic read-only export of all 26 snapshot tables with secret-key redaction, stable row ordering, per-table hashes, and a source-hash manifest; output remains outside target ownership. |
| Raw staging-envelope bridge | `scripts/erp_snapshot_stage_raw.sh` | Consumes only the redacted export, emits one `legacy/raw/<table>` envelope per source row, preserves exporter primary-key identity, and fails closed on secret-shaped fields or missing IDs. |
| Raw staging batch contract | `migration/erp` | Enforces unique table/primary-key and source identities before opaque raw envelopes can enter a manifest. |
| Migration manifest and atomic apply | `migration/manifest` | Per-source disposition, quarantine reason, canonical source-identity checks, target-envelope matching, all-or-nothing store apply, and rollback states. |
| Shadow parity report | `migration/parity` | Count/minor-unit comparison metrics with exact, tolerance, and mismatch classifications plus an explicit acceptance gate. |
| Migration receipt and rollback gate | `migration/run` | Immutable baseline/applied receipt, parity certification, and fail-closed rollback when post-apply writes are detected. |
| Reproducible shadow plan | `migration/shadow` | Derives record-count metrics from the target store and combines them with provided source control totals for repeatable parity certification. |
| Opening control totals | `migration/control` | Validates non-negative, unique domain control totals and compiles them into reproducible shadow expectations for opening-state reconciliation. |
| Budget reservation | `finance` | Reserve, consume, release, and overrun tests. |
| Commitment lifecycle | `operations/commitment` | Draft → submitted → approved → performed → settled plus cancellation guard. |
| ERP contract promotion | `migration/erp` + `operations/commitment` | Contract rows require explicit legal principal, project scope, counterparty identity, and amount-bounded creation grants before becoming commitments. |
| Budget linkage | `operations/commitment` | Explicit commitment-to-budget reservation, consumption, release, currency, and amount checks. |
| Commitment recognition | `operations/commitment` | Performed commitment → expense/payable balanced recognition journal. |
| Commitment/settlement/expense accounting-event adapters | `operations/commitment` + `operations/settlement` + `operations/expense` + `finance/accounting` | Validated source-to-journal events can be appended with source-identity replay protection; posting authorization remains local to the accounting book. |
| Commitment aggregate projection | `operations/commitment` + `persistence/store` | Commitment snapshots serialize to immutable, revisioned projections with identity checks and source-event replay anchors. |
| Settlement and payment projection | `operations/settlement` | Performed commitment → authorized payment release → balanced payable/cash journal. |
| ERP payment-application import | `migration/erp` + `operations/settlement` + `cmd/promote` | A source payment application becomes a requested settlement only against an explicitly replayed performed commitment; source approval/payment flags never release cash implicitly. |
| ERP advance-offset promotion | `scripts/erp_advance_offset_promotion_plan.sh` + `cmd/advance_offset_plan` + `cmd/promote` + `migration/erp` | The export-backed compiler is now native MoonBit and preserves explicit BU/principal/employee/currency joins, positive fixed-point amounts, offset identity/scope/amount bounds, deterministic quarantine reasons, and the `moonproj.erp.advance-offset-promotion-plan.v1` envelope. The fixture promotes one advance plus one explicitly mapped `cb_loan_offset` row; the native importer applies 150,000 minor units only after matching the imported loan, employee, principal, scope, and amount-bounded offset authority. |
| Settlement aggregate projection | `operations/settlement` + `persistence/store` | Requested/approved/released settlement snapshots persist as immutable revisioned projections with source-event anchors. |
| Project lifecycle | `operations/project` | Ordered project stages with scoped authority and lifecycle events. |
| ERP project-master promotion | `migration/erp` + `operations/project` | Project rows require explicit legal-principal/project-scope mappings and matching creation grants; BU relationships are not treated as ownership automatically. |
| ERP lifecycle import | `migration/erp` | Explicit source-stage mapping replays only the ordered current target stage and leaves historical status/progress in the source envelope. |
| Project plan and task dependencies | `operations/project` | Dependency-gated task start, bounded progress, completion, blocking, and cancellation states. |
| ERP task-state import | `migration/erp` | Current `pending`/`in_progress`/`done`/`blocked` state replay is explicit, dependency ordered, and separately authorized from structural task import. |
| Project and plan aggregate projections | `operations/project` + `persistence/store` | Project lifecycle and task-plan snapshots persist stages, tasks, costs, dependencies, and progress as immutable projections. |
| Marketing campaign and placement | `operations/marketing` | Campaign budget, activation/completion, placement lifecycle, lead capture, and spend-cap controls. |
| Supplier and tender control | `operations/procurement` | Supplier qualification/blacklist states and planning → publishing → bidding → award → completion tender flow. |
| Supplier/tender aggregate projections | `operations/procurement` + `persistence/store` | Supplier qualification and awarded tender snapshots persist as immutable revisioned projections with bid/evaluation evidence. |
| Tender award-to-commitment boundary | `operations/procurement` + `operations/commitment` | An awarded qualified-supplier tender can create a draft commitment only through separate `tender:award-to-commitment` and `commitment:create` grants; no award, payment, or accounting effect is inferred implicitly. |
| Contract milestones | `operations/contract` | Time/progress/event-triggered milestone eligibility, achievement, payment, overdue, and cancellation states. |
| Contract milestone projections | `operations/contract` + `persistence/store` | Planned/eligible/reached/paid milestones persist plan, actual amount, trigger, and state as immutable projections. |
| Milestone-to-settlement boundary | `operations/contract` + `operations/settlement` + `persistence/store` | A reached milestone can create a requested settlement with a retained milestone ID through separate milestone and settlement grants; separate immutable milestone and settlement projections preserve the link while approval, release, cash, and accounting remain distinct. |
| Delivery progress and outputs | `operations/delivery` | Evidence-backed progress reports and deliverable acceptance/remediation states. |
| Delivery aggregate projections | `operations/delivery` + `persistence/store` | Accepted progress and deliverable snapshots persist evidence IDs, value/progress, and state as immutable projections. |
| Delivery-to-recognition boundary | `operations/delivery` + `finance/accounting` + `finance/cost` | Accepted, evidence-backed progress supplies the cost-forecast progress input and can construct a validated source-to-journal recognition event; journal posting, cash, tax, and revenue policy remain separate. |
| Dynamic cost | `finance/cost` | Deterministic `B = D + E + F + G` fixed-point component calculation. |
| ERP dynamic-cost promotion | `migration/erp` + `finance/cost` | Cost cohorts retain source cost identity and project scope while validating component arithmetic; duplicate IDs and negative components fail closed. |
| Dynamic-cost aggregate projection | `finance/cost` + `persistence/store` | Four-component breakdown and total persist as immutable source-linked cost envelopes. |
| Cost forecast | `finance/cost` | Target/committed/actual cost forecast linked to accepted progress with signed variance. |
| Cost-forecast aggregate projection | `finance/cost` + `persistence/store` | Progress-linked forecast-at-completion and signed variance persist as revisioned projections. |
| Sales agreement | `operations/sales` | Reserved → signed → fulfilled customer agreement lifecycle. |
| Sales-to-receivable boundary | `operations/sales` + `finance/receivable` | A fulfilled sales agreement can open a receivable through separate sales-recognition and receivable-opening grants; collection cash and revenue accounting remain separate. |
| Invoice-to-receivable boundary | `operations/invoice` + `finance/receivable` | An accepted invoice can open a receivable through separate invoice-recognition and receivable-opening grants, retaining the invoice as source identity; collection cash and revenue accounting remain separate. |
| Customer, reservation, mortgage, refund | `operations/sales` | Customer state, reservation conversion, mortgage approval/release, and refund approval/payment controls. |
| Receivable | `finance/receivable` | Open, partial, complete, and over-collection-controlled customer balances, with explicit opening receivable-to-revenue and separate collection cash-settlement accounting-event adapters. |
| Receivable aggregate projection | `finance/receivable` + `persistence/store` | Open/collected snapshots persist as immutable revisioned projections with source-event anchors. |
| Payable | `finance/payable` | Supplier obligation, partial/full payment, overpayment, and void controls, with explicit opening expense-to-payable and separate payment cash-settlement accounting-event adapters. |
| Payable aggregate projection | `finance/payable` + `persistence/store` | Open/paid/voided snapshots persist as immutable revisioned projections with source-event anchors. |
| Invoice lifecycle | `operations/invoice` | Issue, accept, void, partial payment, full payment, and overpayment controls. |
| Invoice aggregate projection | `operations/invoice` + `persistence/store` | Draft/accepted/paid/voided snapshots persist as immutable revisioned projections with source-event anchors. |
| Employee advance | `finance/employee_finance` | Open, partial repayment, full repayment, and scoped offset controls. |
| ERP employee-advance promotion | `migration/erp` + `finance/employee_finance` | Loan rows require explicit legal principal, employee identity/scope, currency, and amount-bounded creation grants; source BU identity is not reused as ownership. |
| Employee advance projections | `finance/employee_finance` + `persistence/store` | Advance amounts, repayments, employee identity, and balance state persist as immutable projections. |
| Expense claim | `operations/expense` | Multidimensional allocation, approval state, and deterministic advance offsetting. |
| Expense projection and recognition journal | `operations/expense` + `persistence/store` | Allocations, advance offsets, approval state, and balanced expense-recognition journal persist with source identity. |
| Weighted workflow | `operations/workflow` | Step-specific authority, weighted approvals, duplicate-action and wrong-step guards. |
| ERP workflow-definition cohort promotion | `migration/erp` + `operations/workflow` | Process definitions receive isolated ordered source steps and explicit local capability mappings; source labels never become authority implicitly. |
| Workflow definition/instance projections | `operations/workflow` + `persistence/store` | Approval steps, actions, current step, and decision status persist as immutable projections. |
| ERP workflow-assignment cohort | `scripts/erp_workflow_assignment_plan.sh` + `cmd/workflow_assignment_plan` + `cmd/workflow_assignment` + `operations/workflow` + `foundation/access` + `persistence/store` | The export-backed compiler is now native MoonBit and preserves the Python contract: manifest hash validation, explicit process/step/user/mapping joins, deterministic quarantine reasons, assignment weights, and the `moonproj.erp.workflow-assignment-plan.v1` envelope. Six `wf_step_assignee` rows receive explicit actor identities, process scopes, and capabilities; native promotion validates typed process/step attachment evidence, projections persist configuration-only/non-authorizing markers, delegated decisions retain bounded effective-window/revocation evidence, and SLA policies retain due/overdue observation evidence without approval mutation. Exact parity/replay persists the cohort and decision-time capability checks remain mandatory. |
| CBS subject/version | `finance/cbs` | Draft/active/frozen subject dictionary with duplicate-code and target-total controls. |
| CBS aggregate projections | `finance/cbs` + `persistence/store` | Subject versions persist targets, hierarchy, state, and totals as immutable revisioned projections; subject-scoped budget ledgers persist reservation/consumption state separately. |
| CBS cost-subject link | `finance/cbs` + `persistence/store` | Active/frozen CBS versions accept scoped source-to-subject cost links with explicit source identity and immutable link projections; separate budget-ledger reservation/consumption and broader schema/source coverage remain distinct. |
| ERP CBS cost-link cohort | `scripts/erp_cbs_cost_link_plan.sh` + `cmd/cbs_cost_link_plan` + `cmd/cbs_link` + `cmd/cbs_budget` + `persistence/store` | An eighth, independently mapped cohort translates all 7 non-empty `cb_cost` rows into explicit CBS subject links and one deduplicated `cbs_version` configuration projection, persists exact parity, and replays idempotently without budget consumption or accounting posting; both the cost-link and source-budget compilers are now native MoonBit, while the opt-in budget plan still persists subject-scoped control evidence. |
| Agent boundary | `intelligence/agent_port` | MoonClaw-neutral request/result contracts with authority ceiling and idempotency. |
| Warning findings | `intelligence/warning` + `cmd/warning` + `scripts/erp_warning_plan.py` + `persistence/store` | Deterministic cost-overrun finding plus scoped acknowledge/resolve/suppress lifecycle; the source planner scans explicitly named `cb_cost` leaf rows and persists immutable warning evidence with non-workflow, non-cash, and non-accounting markers. |
| Notification outbox boundary | `intelligence/notification` + `cmd/notification` + `ERP_NOTIFICATION_BOUNDARY.md` | Complete locally as a reviewed, authority-checked, source-bound outbox lifecycle for in-app/email/webhook intent. The synthetic plan passes native promotion, exact SQLite parity/replay, and PostgreSQL adapter replay; no provider call, workflow mutation, cash release, or accounting posting is performed. |
| Investment analytics seed | `investment/analytics` | Deterministic moving average and trend fixture translated from Moonfish intent. |
| Investment mandate and proposal | `investment/domain` | Local mandate limits, deterministic analysis attachment, proposal approval, controlled execution, and position creation. |
| Investment mandate/proposal/position projections | `investment/domain` + `persistence/store` | Mandate limits, Moonfish analysis evidence, proposal states, executed positions, validated acquisition journals, and acquisition accounting-event links persist as immutable revisioned evidence. |
| Versioned investment model | `investment/model` + `persistence/store` | ERP version/index rows become a governed local model with duplicate/version checks, source-value preservation, explicit authority, and revisioned model/evaluation projections; numeric/date classification, parent-total reconciliation, and known ratio derivations remain analytics-only. |
| ERP investment-model cohort promotion | `migration/erp` + `investment/model` | Version mappings isolate index rows, require explicit principal/grants, and reject duplicate versions or stray indexes before promotion. |
| Investment portfolio and valuation | `investment/portfolio` + `cmd/investment_benchmark` + `cmd/investment_performance` + `cmd/investment_valuation` | Mandate-bound position book, exposure limits, explicit quote valuation, deterministic period-scoped per-position performance attribution and benchmark active-return comparison, source-snapshot/mapping/evidence-bound external benchmark reconciliation with tolerance and analytics-only projection markers, missing-evidence guards, and gain/loss mark-to-market source-to-journal adapters with explicit valuation authority; the reviewed performance and valuation cohorts now persist analytics and event/link evidence with exact parity/replay, while cash and accounting-book posting remain separate. |
| Portfolio risk scenarios and projections | `investment/portfolio` + `persistence/store` | Shock-based stress reports, loss limits, portfolio position snapshots, and mandate breach flags persist as governed evidence. |
| Reconciliation report | `finance/reconciliation` | Expected-versus-actual lines and journal-side comparison with explicit currency and balance errors. |
| Consolidated reporting evidence | `finance/reporting` + `cmd/consolidated_report` + `persistence/store` | Source-snapshot-bound balanced sections combine into a deterministic `consolidated_report` projection with control totals, mapping versions, accounting-link counts, and explicit non-posting cash/period/tax flags. |
| Tax obligation | `finance/tax` | Rate-based calculation, review, filing, payment, voiding, authority-scoped events, and a reviewed-state tax-obligation recognition-event adapter that produces a balanced source-to-journal link without posting. |
| Tax obligation projections | `finance/tax` + `persistence/store` | Jurisdiction/category, rates, calculated amounts, filing state, and source references persist as immutable projections. |
| Tax filing aggregate | `finance/tax` + `persistence/store` | Reviewed obligations can create a separate period/authority-referenced filing record with prepared/submitted/accepted/rejected state and immutable projections; tax payment and ledger posting remain separate. |
| Cash and bank movement | `finance/treasury` | Account balance, inflow/outflow approval, release, overdraw protection, and reconciliation. |
| Cash account/movement projections | `finance/treasury` + `persistence/store` | Balances, bank references, release state, direction, and reconciliation events persist as immutable projections. |
| Bank statement import/reconciliation | `finance/treasury` + `persistence/store` | Statement lines validate account/currency/balance invariants, match released or reconciled cash movements exactly once, and can then match each line to a balanced accounting event by source identity; the aggregate persists imported/reconciled/ledger-reconciled evidence without releasing cash or posting journals. |
| Cash planning and dispatch | `finance/treasury` | Planned/confirmed/actualized cash plans and approved project-to-project fund dispatch. |
| Corporate financing facility | `finance/financing` | Facility approval, activation, draw limit, interest accrual, repayment, closure, and default controls, plus explicit draw/repayment source-to-journal adapters with stable action IDs; accounting-book posting remains separate. |
| Financing facility projections | `finance/financing` + `persistence/store` | Limits, draws, outstanding principal, rates, repayment, and default state persist as immutable projections. |
| Asset lifecycle and depreciation/disposal | `finance/assets` | Capitalization, activation, impairment/disposal states, residual-value controls, deterministic monthly depreciation, balanced depreciation and derecognition journals, revisioned asset-register projections, and depreciation/disposal accounting-event links. Period close integration and production asset-import cohorts remain pending. |

## Current verification

The current scaffold has 245 passing MoonBit tests across the new packages. The
CLI demonstrates an authorized commitment through settlement and journal
validation, followed by a manifest-to-store migration apply and derived shadow
parity certification; it also reports the sanitized backup inventory as 26
tables, 120 rows, 19 rows covered by mapped import seams, and 101 rows covered by
typed preservation envelopes; the production-oriented SQL catalog has four
versioned gates. The ERP
fixture importer exercises representative project,
contract, and dynamic-cost translation. Snapshot/file persistence, tax
calculation, cash release, financing, investment approval, portfolio valuation, delivery evidence,
cost forecasting, backup/restore, accounting-event replay protection, audit replay protection, and reconciliation are covered by
package tests.
Delivery tests now require accepted, evidence-backed progress before creating
either a cost forecast input or a balanced delivery recognition event, and the
event can be appended as a source-identifiable accounting record without
posting it. CBS tests likewise require an active or frozen version and an
existing subject before a scoped source-to-subject cost link can be projected.
Opening control totals now compile into shadow expectations and reject duplicate
or negative controls before certification. The SQLite backup can now be exported
into a credential-safe, hashed row bundle for relationship review and later
typed/domain import, without putting raw source payloads into the repository.
The redacted fixture stages all 120 rows with 120 unique source IDs; those rows
remain quarantine evidence until a domain importer and authority grant promote
them. The SQLite rehearsal persists all 120 envelopes durably and reopens the
database with 120 unique records and one migration receipt; a second replay
inserts zero rows. The mapped-cohort planner produces 19 ready candidates with
the reviewed fixture mapping and quarantines 3 items when counterparty/employee
mappings are removed.
The native domain-promotion command then applies the complete plan and emits a
receipt with 7 organization units, 2 projects, 2 commitments, 7 costs, and 1
advance; it refuses the incomplete plan before writing a receipt.
The end-to-end wrapper now applies that domain receipt to 19 immutable SQLite
aggregate projections in one transaction, records a projection receipt, and
replays it with zero inserted projections and `PRAGMA integrity_check=ok`.
The projection parity report then classifies the cohort as `shadow_verified`
with exact counts of 7 organization units, 2 projects, 2 commitments, 7 costs,
and 1 advance; applying a workflow receipt to that database produces a visible
missing/extra mismatch instead of a false pass.
The accounting-link plan/native command then validates 2 explicit commitment
journals plus 1 employee-advance opening journal and the SQLite adapter
persists 3 source-to-journal links with `integrity=ok`; a second apply inserts
zero links. This is traceability only:
cash release, accounting-book posting, and subledger recognition remain
separate authority-bearing events.
The wrapper also backs up the final database and reopens the restored file;
the fixture reports `backup_restore_verified` with equal logical digests across
120 raw records, 19 projections, 3 accounting links, and 3 migration receipts.
With the fifth offset-cohort mapping, the same end-to-end run reaches 21
projections, 4 accounting links, and 5 migration receipts; both the economic
and offset cohorts report `shadow_verified`.
The shared SQLite driver smoke gate also proves rollback (`0` rows) and commit
(`1` row) on an isolated database with integrity `ok`; the committed row uses
the exact parameterized SQL command emitted by `persistence/sql`.
The managed-production deployment validator now rejects raw DSNs and incomplete
TLS, pooling, backup, restore, rollback, observability, or approval manifests;
the checked-in example remains owner-unapproved and therefore cannot authorize
deployment.
The cutover gate combines those checks into `cutover-gate.json`: the full
fixture is technically ready for business acceptance, but it deliberately
does not authorize ownership transfer while the project-1 task-state,
managed-production deployment, and 49 schema-only-table scope exceptions
remain open. The gate includes a `schema_scope` check so every rehearsal
records the 75/26/49 boundary explicitly.
The schema-cohort planner also records all 49 absent tables in seven ordered
waves, and the cutover gate verifies that planning artifact without treating it
as imported data.
The source-side relationship audit now runs before raw staging because the
legacy schema declares no foreign keys; it checks 60 reviewed relationships and
216 non-empty references with zero orphan values, and the gate records that
result as `relationship_integrity` evidence.
Its accounting reconciliation check requires every supplied reviewed link cohort
to report `reconciled` without cash release or period posting. The extended
seventh-argument run reaches 7 durable links and 16 migration receipts.
The same command promotes the typed workflow cohort as 2 definitions/12 steps
and refuses a plan with a missing capability mapping.
The lifecycle planner/command promotes 2 project masters plus their 2 ordered
lifecycle cohorts, yielding final stages `development` and `design`; a missing
stage mapping is refused before output.
The task planner/command promotes 2 dependency-ordered task structures (7 and
2 tasks) while leaving source status/progress in typed evidence; a missing
principal is refused before output.
The investment planner/command promotes 1 model with 26 indexes while
preserving source value representations; a missing principal is refused before
output.
The payment planner/command replays 2 explicit commitment state paths, creates
4 planned milestones and 3 requested settlements without releasing cash;
removing one state map is refused before output.
The native milestone boundary also persists a reached milestone and its
requested settlement as separate projections with the milestone ID retained;
approval, release, cash, and accounting remain independent events.
The user planner/command promotes 5 credential-free identities; the legacy
super-user account is represented without granting target super-user authority.
The audit planner/command promotes 2 login audit records only with explicit
target interpretation and actor grants; removing those mappings is refused
before output.
The parameter planner/command promotes 2 dictionaries with 8 options,
including the explicit `vys_proceeding` expense catalog; removing either
principal/scope mapping is refused before output.
The task-state planner identifies the two `proj-0001` dependency conflicts and
refuses the full mixed cohort; a project-2-only plan proves 2 state replays
and its project, task-plan, and task-state receipt persists as 3 exact-parity
projections. The inconsistent project remains quarantine evidence.
The separate advance-offset planner/native command promotes the fixture's
`loan-001` plus `off-001` cohort, applies 150,000 minor units only after the
target advance is created, and its two projections pass exact parity. Missing
or mismatched offset mappings are quarantined before any receipt is written.
The typed-cohort rehearsal then promotes and durably projects 34 additional
business items across workflow, lifecycle, task structure, investment, payment,
credential-free users, audit, parameter, and clean project-2 task-state
cohorts, plus 40 redacted typed-evidence rows from task snapshots/reports,
workflow assignees, lifecycle-instance history, lifecycle-stage catalog, and
proceeding catalog, plus one quarantined project-1 task-state observation, for
75 items before evaluation. The source-bound investment evaluation adds one
analytics-only projection, for 76 typed items in total. Each cohort has its own
mapping version and exact parity report; the complete database reaches 51
projections and 13 migration receipts before task-state; with the clean
project-2 state, observed exception, typed-evidence, and evaluation cohorts it
reaches 97 projections and 17 receipts when advance-offset is supplied. A
full run without advance-offset reaches 95 projections; a second full run is
idempotent.
With the optional eighth CBS cost-link mapping, the same target database
reaches 103 projections and 17 migration receipts; all seven `cb_cost` links
and the deduplicated `cbs_version` configuration report exact
`shadow_verified` parity and a second apply inserts zero rows.
With the optional ninth workflow-assignment mapping, it reaches 109 projections
and 18 migration receipts; all six assignee rows report exact parity and a
second apply inserts zero rows. Assignments remain configuration, not
authority. The typed investment cohort then adds one
`investment_model_evaluation` projection and one mapping-scoped receipt; the
next complete typed run therefore reaches 110 projections.
The assignment planner refuses missing source users, target identities,
processes, scopes, or capability mappings before native promotion.
With the optional eleventh delivery-progress mapping, the evaluation-aware
rehearsal reaches 111 projections and an exact `progress_report` parity/replay result;
the native receipt records `acceptance_created=false` and
`recognition_created=false`. The current complete SQLite run also derives a
source-bound CBS budget plan from the five positive `cb_cost.dfs_budget`
amounts, reaching 112 projections, 7 durable accounting links, and 21
migration receipts; the CBS budget ledger remains reservation evidence with
accounting and cash effects false. A source-bound warning scan over the two
explicit positive `cb_cost` component overruns adds one warning projection and
reaches 113 projections and 22 receipts while notification, workflow, cash,
and accounting effects remain false. The optional reviewed notification
cohort adds one authority-checked outbox projection, reaching 114 projections
and 23 receipts without provider, workflow, cash, or accounting effects. The
reviewed access cohort adds one explicit `access_directory` projection,
reaching 115 projections and 24 receipts while keeping workflow, cash, and
accounting effects false.
Access tests also reject incompatible role assignments both when a new role is
assigned and when a separation rule is added after existing assignments.
They also prove delegation effective windows, amount ceilings, revocation, and
wrong-actor rejection.

This does not yet prove full ERP parity, production accounting posting,
durable target-owned persistence/economic acceptance of the promoted cohort,
promotion of the remaining typed-staged rows, or production readiness.

## Next implementation slices

1. Build on `docs/ERP_UI_PARITY_MATRIX.md` / `.json`, the executable
   source-to-target page/route/action parity matrix for all 56 browser routes,
   30 ERP route files, and 338 handlers. Finish the Rabbita
   visual/interaction gaps, including exact source navigation,
   empty/error/loading states, forms, and responsive behavior; record
   screenshots and scenario results per route.
2. Close procurement acceptance before opening another broad surface. The
   local supplier lifecycle/risk reads, tender planning/award/complete, and
   contract-split reads/creates now have PostgreSQL smoke and Rabbita command
   state evidence. Source tender create/split aliases now translate legacy
   fields, return source-shaped responses, merge command projections into
   source reads, and pass service/gateway replay smoke. Explicitly decide
   whether source arbitrary-state writes, imported tender deletion, and
   standalone award rows are retained as gated source behavior or receive
   owner-approved local projections. The source signature-check now has an
   explicit missing-provider/populated-provider gate plus a command-owned
   derived preview; remaining procurement work is populated-provider decision
   parity, external risk-rescore integration, source-backed
   rows, browser acceptance, award-to-commitment acceptance, and
   procurement-owner sign-off. Imported rows remain read-only.
3. Replace the local credential/session adapter with the reviewed production
   identity, token issuer, rotation, persistence, and owner-acceptance boundary
   before calling any connected slice accepted. Do not count fixture-backed
   buttons as complete behavior.
4. Request, receive, and validate the missing 49-table credential-safe
   MySQL/JSON export. The current 26-table/120-row fixture remains useful for
   rehearsal but cannot support full ERP parity or cutover.
5. Expand the connected runtime slice beyond the now-verified local
   sales/receivables reads and lifecycle commands through delivery,
   treasury/financing, tax/close,
   reporting/notifications, and investment. Reuse existing native cohorts and
   parity gates; do not create another standalone hardening slice unless it is
   required by the active workflow.
6. Reconcile the quarantined project-1 task-state/progress exception with a
   named business owner and preserve the observed evidence until a decision is
   recorded. Then run role-based UAT and a read-only shadow period for each
   accepted wave.
7. Put the already-tested PostgreSQL adapters behind the approved managed
   service, backup/restore, monitoring, and rollback runbooks only after the
   functional/API and source-completeness gates are ready.
